# Changelog

## Unreleased

### Hinzugefuegt

- `XdtBox.LicenseManager` verwaltet jetzt Kunden aus Lizenzanfragen: Import legt Kunden ueber Kundennummer oder InstallationId an beziehungsweise aktualisiert sie, Bank-/SEPA-/Rechnungsdaten werden lokal gespeichert, ein Netto-Einzelpreis pro Geraeteanbindung berechnet Kunden- und Gesamtkosten, Kundendetails zeigen aktuelle Anbindungen und Lizenzhistorie, die Kundenliste kann als PDF exportiert werden, und eine LicenseManager-Sicherung speichert Kunden, Preis, Einstellungen und Historie ohne private Schluessel. Die bestehende Lizenzsignatur-, Private-Key- und `.xdtboxlic`-Erzeugung bleibt unveraendert; alte Lizenzanfragen ohne neue Felder bleiben importierbar.
- Kleine UI-Feinkorrektur fuer die finale 1.0-Oberflaeche: `AIS Ausgabe Info` und `Notizen Techniker` verwenden fuer `Schliessen` beziehungsweise `Speichern` wieder den hellen Standardbutton statt Primary-Dunkelblau. Im Tab `Schnittstellenprofile` heissen die Ordnerbuttons nun `Ordner waehlen` und stehen direkt neben den zugehoerigen Eingabefeldern in Auto-Spalten. Es wurde keine Parser-, MEDISTAR-/XDT-, Geraetekommunikations-, Lizenz- oder Installer-Fachlogik geaendert.
- UI-Nachkorrektur fuer die aktuelle 1.0-Oberflaeche: `Geraet laden` startet hoeher, damit die Bildaktionen und `Schliessen` ohne manuelles Vergroessern sichtbar bleiben. Im Tab `Schnittstellenprofile` liegen `Ordnerbereinigung`, `Archivierung` und `Pruefung vor Aktivierung` in einem eigenen Sicherheitsbereich; Status, Buttons, Hinweistext und Tabellen der Aktivierungspruefung sind getrennte Zeilen und ueberlagern sich nicht mehr. `Notizen Techniker` nutzt Symbolschalter fuer Text-/Handmodus, laesst bestehende Texte per Doppelklick wieder direkt bearbeiten und zeigt Auswahlrahmen/Griffe nur im Handmodus. Im Lizenz-Tab stehen SEPA-Zustimmung und Rechnung-ohne-SEPA direkt nebeneinander, der Rechnungsversand bleibt separat und `Speichern` nutzt wieder den ruhigen Standardbutton. Fachlogik fuer Parser, MEDISTAR-/XDT-Ausgabe, Geraetekommunikation, Lizenzpruefung und Installer-Datenpolitik blieb unveraendert.
- UI-Nachkorrektur nach Sichtpruefung: Das Fenster `Geraet laden` ist hoeher und ordnet `Bild auswaehlen`, `Bild zuruecksetzen` und `Speichern` in einer gemeinsamen Buttonzeile an, waehrend `Schliessen` separat rechts unten bleibt. Im Tab `Schnittstellenprofile` liegen `Ordnerbereinigung`, `Archivierung` und `Pruefung vor Aktivierung` wieder in eigenen Grid-Zeilen ohne Ueberlagerung. Der Tab `Notizen Techniker` besitzt jetzt Text-/Handmodus fuer direkte Whiteboard-Texteingabe, Auswahl/Verschieben/Groessenaenderung sowie ein Kontextmenue fuer Kopieren, Einfuegen und Entfernen. Der Lizenz-Kundendatenbereich ist in Praxis/Firma, Kontakt, Rechnungsversand und Bank/SEPA getrennt. Fachlogik fuer Parser, MEDISTAR-/XDT-Ausgabe, Geraetekommunikation, Lizenzpruefung und Installer-Datenpolitik blieb unveraendert.
- Zentrales UI-Polish fuer XDTBox 1.0: `XdtBoxTheme.xaml` enthaelt nun die gemeinsame XDTBox-Farbwelt, Corner-Radien, Karten-, Button-, Tab-, Eingabe-, DataGrid- und Toggle-Styles. MainWindow, AIS-Ausgabe-Info, Geraetesteckbrief, App-Einstellungen und der Techniker-Notizen-Whiteboardbereich nutzen diese zentrale Optik mit abgerundeten Karten, dezenter Plastizitaet, modernen Hover-/Pressed-Zustaenden und ruhigen Praxissoftware-Farben. Es wurde keine Parser-, MEDISTAR-/XDT-, Geraetekommunikations-, Lizenz- oder Installer-Fachlogik geaendert.
- UI-Ausbau fuer XDTBox 1.0: Der Tab `Schnittstellenprofile` zeigt jetzt das Geraetebild des gewaehlten Geraeteprofils, die Buttons `AIS Ausgabe Info` und `Geraetesteckbrief` sowie einen grossen Aktivierungsstatusbutton (`Aktivierte Anbindung` / `Anbindung deaktiviert`) statt der frueheren Aktivierungscheckbox. Der XDT-Baukasten besitzt ebenfalls einen `Geraetesteckbrief`-Button in der Geraeteidentitaet; beide Buttons laden den Steckbrief ueber `deviceProfileId`, App-Originaldaten und lokale Overrides. Standardbuttons nutzen eine dezente XDTBox-blaue Optik, waehrend der Aktivierungsbutton farblich gruen/rot getrennt bleibt.
- Neuer Kundendatenbereich `Notizen Techniker`: Ein Whiteboard-Tab erlaubt freie Textnotizen, Bildimport per Dateiauswahl oder Drag-and-Drop, Positionieren/Groessenanpassung von Text- und Bildelementen, Formatierungen, Einfuegen aktiver Geraete, Speichern/Laden unter `%LocalAppData%\XdtDeviceBridge\technician-notes` und PDF-Export. Sicherung/Umzug nimmt Whiteboard-Status und kopierte Bilder auf; der Installer packt diese lokalen Notizen nicht ein.
- Optionaler Tab-Schutz ergaenzt: Bis auf `Verarbeitung` koennen die Haupttabs lokal gesperrt werden. Das Passwort wird nicht im Klartext gespeichert, sondern mit Salt und PBKDF2-Hash unter `%LocalAppData%\XdtDeviceBridge\ui\tab-protection.json`; ein interner Support-Override ist nur nutzbar, wenn ein Anwenderpasswort konfiguriert wurde. Sicherung/Umzug nimmt die Tab-Schutz-Konfiguration auf.
- Lizenz-Kundendaten erweitert: Lizenzanfragen und lokale Lizenz-Kundendaten enthalten jetzt optional Rechnungs-E-Mail, IBAN, BIC, Kontoinhaber, SEPA-Lastschriftzustimmung und Rechnung-Option. SEPA und Rechnung werden in der UI gegenseitig abgegrenzt, ohne die bestehende Lizenzsignaturpruefung zu veraendern.
- Aktueller Pruefstand fuer den UI-Ausbau: `dotnet build XdtDeviceBridge.sln` erfolgreich mit 0 Warnungen und 0 Fehlern, `dotnet test XdtDeviceBridge.sln` mit 1984 erfolgreichen Tests, 0 fehlgeschlagen und 0 uebersprungen. `scripts\build-xdtbox-installer.ps1` erzeugt `artifacts\installer\XDTBox_Setup_1.0.exe` mit 79.314.259 Bytes; die Publish-App startet erfolgreich aus `artifacts\publish\XDTBox`.
- Geraete-Steckbriefe fuer alle 64 aktuellen BuiltIn-Geraete vorbereitet: Die Originaldaten liegen als App-Asset unter `XdtDeviceBridge.App\Assets\DeviceInfo\device-technical-profiles.de.json`, sind eindeutig ueber `deviceProfileId` den BuiltIns zugeordnet und verweisen auf die offiziellen BuiltIn-Geraetebilder. `DeviceTechnicalProfileService` fuehrt Original und lokale Anpassungen aus `%LocalAppData%\XdtDeviceBridge\device-info-overrides.json` zusammen, speichert Techniker-Notizen als Kundendaten und kann lokale Anpassungen auf das Original zuruecksetzen. Die WPF-Steckbriefansicht zeigt Bild, Basisdaten, Kurzbeschreibung, Messwerttabelle, technische Hinweise und eine lokale Techniker-Notiz; sie ist aus `Schnittstellenprofile` und `XDT-Baukasten` erreichbar. Sicherung/Umzug nimmt die lokalen Steckbriefanpassungen auf, die Installationsdatenpolitik trennt App-Originale und Kundendaten, und `docs\GERAETE_STECKBRIEFE.md` enthaelt eine nach Hersteller gruppierte Pruefansicht ohne interne Ausgabe-Syntax.
- Aktueller Pruefstand fuer den Geraete-Steckbrief-Stand: `dotnet build XdtDeviceBridge.sln` laeuft mit 0 Warnungen und 0 Fehlern, `dotnet test XdtDeviceBridge.sln` mit 1976 erfolgreichen Tests, 0 fehlgeschlagen und 0 uebersprungen. `scripts\build-xdtbox-installer.ps1` erzeugt `artifacts\installer\XDTBox_Setup_1.0.exe` mit 79.299.177 Bytes; die Publish-App startet erfolgreich aus `artifacts\publish\XDTBox`.
- `AIS Ausgabe Info` zeigt zusaetzlich die empfohlene Standard-Zeilenbenennung fuer das MEDISTAR-XDT-Setup. Die Liste wird aus dem Schnittstellenprofil und den aktiven Exportregeln abgeleitet und zeigt nur Zeilenkennung plus Messart, z. B. `V0 Lensmeter`, `V1 Autorefraktor`, `V2 Phoropter`, `V4 subjektive Refraktion`, `V7 Keratometer`, `V8 Biometrie`, `Y Tonometrie` und `P Pachymetrie`. XDTBox erzeugt diese Zeilenkennungen nicht selbst und zeigt dort keine Beispielwerte, SourcePaths oder Platzhalter.
- Referenzpaket Batch 8 umgesetzt und um ZEISS IOLMaster 700 ergaenzt: Canon RK-F2, Canon TX-20P, ZEISS VISULENS 550, ZEISS VISUPLAN 500, ZEISS VISUREF 100, ZEISS IOLMaster 700, Visionix Retinomax 5, Visionix VX 120 und Visionix VX 650 sind als defensiv abgeleitete BuiltIns mit eigenen Geraete-, Export- und Schnittstellenprofilen vorbereitet. `CanonZeissVisionixDeviceParser` liest REF/Lensmeter nach `6228`, KM nach `6221`, Tonometrie nach `6205` und Pachymetrie/CCT nach `6220`; `ZeissIolMaster700DeviceParser` liest IOLMaster-XML mit VKT/AL nach `6227` und R1/R2-Keratometrie nach `6228`. Baukasten und manuelle Verarbeitung akzeptieren die Daten ohne fremde Referenzartefakte. Canon OCT A-1 Xephilo und Visionix Optovue iVue 80/100 bleiben bewusst ausgeschlossen.
- Schnittstellenprofile nach Praxistest nachgeschaerft: `AIS Ausgabe Info` markiert bei reinen Dokumentgeraeten (`MEDISTAR + Dokumentanhang`, `MEDISTAR + Manuelle Dokumentuebergabe`) die Linkfelder `6302`, `6303` und `6305` als Karteikarten-relevant, waehrend optionale Anhaenge bei Messgeraeten optional bleiben. Die Untersuchungsart-Anzeige heisst nun `Untersuchungsart`, hebt den `8402`-Hinweis sichtbar hervor und nutzt korrigierte Defaults (`LENS`, `PHORO`, `AUTO`, `KERA`, `KOMB`, `TONO`, `PACHY`, `ENDO`, `OCT`, `DOKU`, `MESS`). Die Defaultlogik priorisiert die Geraeteidentitaet vor optionalen Anhangsfaehigkeiten: Tonometer/Pachymeter wie NIDEK NT-1E bleiben `TONO`, `DOKU` gilt nur fuer reine Dokument-/AttachmentOnly-Profile. Herstellerfilter, AIS-Filter und Schnittstellenprofil-Auswahl stehen in einer kompakten Kopfzeile; das Profil-Dropdown ist begrenzt. Das `6305`-Layout im Tab `Schnittstellenprofile` trennt Pfadfeld und Ordnerbuttons sauber in eigenen Grid-Spalten. Im Lizenz-Tab gibt es `Lizenz entfernen`; entfernt werden nur lokale Lizenzdateien, Profile/Kundendaten/Anfragen bleiben erhalten, und Lizenzanfragen bilden weiterhin den aktuellen Gesamtzustand der Einrichtung ab.
- Aktueller Pruefstand fuer die Schnittstellenprofil-/AIS-Ausgabe-Info-Korrektur: `dotnet build XdtDeviceBridge.sln` laeuft mit 0 Warnungen und 0 Fehlern, `dotnet test XdtDeviceBridge.sln` mit 1933 erfolgreichen Tests, 0 fehlgeschlagen und 0 uebersprungen. Der Installer-Build erzeugt `artifacts\installer\XDTBox_Setup_1.0.exe` mit 63.430.332 Bytes; die Publish-App startet erfolgreich aus `artifacts\publish\XDTBox`.
- Schnittstellenprofil-Ansicht bereinigt: Der fruehere `Lizenzpflichtig`-Haken ist entfernt, weil aktive Schnittstellenprofile immer als Geraeteanbindung zaehlen. Legacy-Profile mit `IsLicenseRequired = false` koennen die Lizenzzaehlung nicht mehr umgehen; das Feld bleibt nur noch fuer Rueckwaertskompatibilitaet erhalten. Die Schnittstellenprofil-Auswahl besitzt jetzt Filter fuer `Geraetehersteller` und `AIS-System`, die `6305`-Layoutzeile ueberlappt nicht mehr mit `Ordner Default`/`Ordner anlegen`, und neue BuiltIn-/Template-/Repair-Defaults setzen Ordnerbereinigung aktiv, Archivierung aus und Fehlerablage aktiv, ohne UserDefined-Entscheidungen ungefragt zu ueberschreiben.
- `AIS Ausgabe Info` im Tab `Schnittstellenprofile` ergaenzt: Der Dialog zeigt zum aktuell gewaehlten Schnittstellenprofil AIS-, Geraete-, Export- und Profilmetadaten sowie eine reine AIS-Ausgabetabelle mit Feldkennung, Bedeutung, AIS-Relevanz, Karteikarten-Sichtbarkeit und Hinweisen. Die Untersuchungsart `8402` wird weiter aus AIS uebernommen und unveraendert zurueckgegeben; empfohlene Defaults fuer neue Profile reichen von `LENS`, `PHORO`, `AUTO`, `KERA`, `KOMB`, `TONO`, `PACHY`, `ENDO`, `OCT`, `DOKU` bis `MESS`, ohne eine eingehende Untersuchungsart fachlich zu blockieren.
- Referenzpaket Batch 7 umgesetzt: Reichert 7CR NCT, Reichert LensChek Plus und Rodenstock CX 800 sind als defensiv abgeleitete BuiltIns mit eigenen Geraete-, Export- und Schnittstellenprofilen vorbereitet. `ReichertDeviceParser` liest Tonometrie nach `6205` sowie Lensmeter-Daten nach `6228`; `RodenstockDeviceParser` liest CX-800-REF nach `6228` und KM nach `6221`. Die Baukasten- und manuelle Verarbeitung akzeptieren diese Textdaten ohne XML-Zwang, synthetische neutrale Fixtures sichern Parser und Export ab, und Phoromat 2000 sowie die Möller-Wedel-Visutron-Familie bleiben wegen widerspruechlicher beziehungsweise nicht ausreichend belastbarer Datenlage dokumentierte Kandidaten.
- NIDEK-Referenzpaket Batch 6 umgesetzt: AR-1, AR-1S und AR-310A sind als Varianten der NIDEK-XML-Autorefraktor-Familie vorbereitet, LM-1800PD nutzt die bestehende Lensmeter-XML-Familie, und NT-1, NT-1E, NT-1P, NT-510 sowie NT-530 nutzen die NT530P-XML-Familie. Die neuen BuiltIns besitzen eigene Geraete-, Export- und Schnittstellenprofile, inaktive Templatepaket-Kandidaten und synthetische neutrale Fixtures. AR-Medianwerte werden ueber `6228`, AR-1S-SR-Werte ueber `6227`, Lensmeter ueber `6228`, Tonometrie ueber `6205` und Pachymetrie ueber `6220` ausgegeben; `6330` und kuenstliche Trennzeilen werden nicht erzeugt. CEM-530, TONOREF II/III und script-/RDD-basierte Altgeraete bleiben dokumentierte Kandidaten, bis echte Rohdaten oder belastbare Protokollinformationen vorliegen.
- TOPCON-Referenzpaket Batch 5 umgesetzt: CL-300PDL, RM-800 und TRK-3 Omnia sind als belastbar abgeleitete BuiltIn-Varianten mit eigenen Geraete-, Export- und Schnittstellenprofilen vorbereitet. CL-300PDL nutzt die bestehende CL-300-Lensmeter-XML-Familie, RM-800 die vorhandene TOPCON-REF-XML-Familie und TRK-3 Omnia die TRK-2P-Mehruntersuchungsfamilie fuer REF/KM/TM/CCT. Die Parser-Erkennung akzeptiert die neuen ModelName-Aliase, der Baukasten kennt die Aliase als warnarme Varianten, synthetische neutrale Fixtures sichern `6228`, `6221`, `6220` und `6205` ab, und selektive Templatepaket-Tests sind vorhanden. EZ-200 Advance und RM-8000 bleiben wegen fehlender echter Rohdaten als dokumentierte serielle Parser-Kandidaten offen; CV-5000/CV-5000S wurde nur statisch bestaetigt und fachlich nicht veraendert.
- Shin-Nippon-Referenzdatenbatch umgesetzt: Accuref R-800, Accuref K-900, DL-1000, DL-800, DL-900, NCT-200 und SLM-4000 sind als testseitig abgesicherte serielle Text-BuiltIns vorbereitet. Der neue `ShinNipponDeviceParser` liest aus neutral abgeleiteter Referenzlogik REF-, KM-, Lensmeter- und Tonometrie-Token, erzeugt MEDISTAR-Zeilen fuer `6228`, `6221` und `6205`, akzeptiert die Textdaten im Baukasten und in der manuellen Verarbeitung ohne XML-Zwang und bringt synthetische Fixtures sowie Templatepaket-Tests mit. DR-900 bleibt wegen unzureichend belegter bidirektionaler Rueckgabe-/Sendeframes offen. Entpackte Analyseartefakte wurden aus dem Repository entfernt; ein Regressionstest scannt dynamisch auf die vier gesperrten externen Marker, ohne sie als Klartext im Repository zu speichern.
- Installer-Build gegen Sandbox-/Publish-Fehler abgesichert: `scripts\build-xdtbox-installer.ps1` baut Publish und Setup jetzt zuerst unter `artifacts\staging\xdtbox-installer` und ersetzt `artifacts\publish\XDTBox` sowie `artifacts\installer` erst nach erfolgreichem `dotnet publish`, sauberer Kundenpublish-Validierung und erfolgreichem Inno-Build. Fehler wie fehlender SDK-Zugriff loeschen damit nicht mehr das letzte funktionierende Setup; externe Tool-Exitcodes werden jetzt hart ausgewertet. Der aktuelle Installer wurde am 2026-06-05 unter `artifacts\installer\XDTBox_Setup_1.0.exe` mit 63.430.332 Bytes erzeugt; die Publish-App startete erfolgreich aus `artifacts\publish\XDTBox`.
- Finaler lokaler EM-3000/EM-4000-Pruefstand uebernommen: Marc hat am 2026-06-05 den vollstaendigen Solution-Build lokal erfolgreich ausgefuehrt (`dotnet build XdtDeviceBridge.sln`, Core/Infrastructure/LicenseIssuer/LicenseManager/Tests/App erfolgreich, Dauer 18,9 s) und den vollstaendigen Testlauf bestaetigt (`dotnet test XdtDeviceBridge.sln`, 1816 Tests erfolgreich, 0 fehlgeschlagen, 0 uebersprungen, Testdauer 12,4 s, Gesamt 16,4 s). Nach dem Installer-Staging-Umbau wurde `scripts\build-xdtbox-installer.ps1` erfolgreich ausgefuehrt und `artifacts\installer\XDTBox_Setup_1.0.exe` neu erzeugt.
- TOMEY-Referenzdatenbatch erweitert: EM-3000 und EM-4000 sind jetzt als testseitig abgesicherte BuiltIn-Kandidaten vorbereitet. Der neue `TomeyEmDeviceParser` liest die abgeleiteten CSV-Token fuer Endothel-/Zellmessungen, mappt Messwerte nach `6228`, Kommentare nach `6227` und Bild-/Dateiverweise nach `6302`; Baukasten, manuelle Verarbeitung, Profilkatalog, Templatepaket-Tests und synthetische Fixtures sind angebunden. TAP-2000 wurde erneut statisch geprueft: COM-Parameter und Framebausteine sind dokumentiert, aber ohne echte Rueckgabe- und Live-Sendeframes weiterhin nicht produktiv freigegeben.
- TOMEY-Referenzdatenbatch umgesetzt: Neue BuiltIns fuer CF-2000, TL-2000C, TL-6000, TL-7000, MR-6000 und TOP-1000 sind als testseitig abgesicherte Kandidaten vorbereitet. `TomeyDeviceParser` liest aus neutral abgeleiteter Referenzlogik serielle CF-2000-Lensmetertexte, TL-CSV-Lensmeterdateien sowie MR-/TOP-XML-Messdaten, erzeugt vorbereitete MEDISTAR-Zeilen fuer `6228`, `6221`, `6205` und `6220`, akzeptiert die TOMEY-Daten im Baukasten und in der manuellen Verarbeitung ohne XML-Zwang und bringt synthetische Fixtures sowie Templatepaket-Tests mit. AP-2500 und TAP-2000 bleiben dokumentierte Kandidaten, weil Messwertrohstruktur beziehungsweise bidirektionale Phoropter-Frames noch nicht belastbar genug fuer BuiltIns sind.
- Huvitz-Referenzdatenbatch umgesetzt: Neue BuiltIns fuer HRK-8000A, HRK-9000A, HNT-1P und HTR-1A sind als serielle Textprofile vorbereitet. `HuvitzTextDeviceParser` liest aus neutral abgeleiteter Referenzlogik REF/KM/Tonometrie/Pachymetrie-Token, erzeugt vorbereitete MEDISTAR-Zeilen fuer `6228`, `6221`, `6205` und `6220`, akzeptiert die Huvitz-Textdaten im Baukasten und in der manuellen Verarbeitung ohne XML-Zwang und bringt synthetische Fixtures sowie Templatepaket-Tests mit. HDR-7000/HDR-9000 und HLM-1/HLM-7000P/HLM-9000 bleiben dokumentierte Kandidaten, weil fuer Phoropter-/Lensmeter-Rohstruktur und Live-Frames noch belastbare Praxisdaten fehlen.
- Referenzdaten-Geraetebatch ausgewertet und erste sichere BuiltIns daraus ergaenzt: NIDEK ARK-510A, NIDEK ARK-560A und NIDEK LM-1800P besitzen nun eigene Geraete-, Export- und Schnittstellenprofile mit Tests. TOPCON KR-800 wird als Alias der bestehenden KR800S-XML-Familie erkannt, und die NIDEK-Lensmeter-Fachlogik akzeptiert LM-1800P-Augenbloecke `I`/`S` als rechts/links. Die neue Doku `docs/REFERENZPAKET_GERAETE_AUSWERTUNG.md` dokumentiert 100 statisch gepruefte Geraeteordner, ausgeschlossene Hersteller, nicht implementierte Kandidaten und scriptbasierte Parser-Kandidaten fuer Folge-Batches.
- XDTBox auf Version `1.0` gesetzt und einen reproduzierbaren Kundeninstaller erstellt: `installer/XDTBox.iss` baut mit Inno Setup 6 ein klassisches Windows-Setup `XDTBox_Setup_1.0.exe` fuer Windows 10+, Defaultpfad `C:\XDTBox`, Startmenue-/optionale Desktop-Verknuepfung, Programme-&-Features-Eintrag, Update-Erkennung ueber Registry/Marker/App-Datei und Deinstallation Variante B. `scripts/build-xdtbox-installer.ps1` veroeffentlicht die Kunden-App self-contained fuer `win-x64`, prueft die Publish-Ausgabe auf ausgeschlossene Hersteller-Lizenztools/private Schluessel und startet anschliessend den Inno-Build. Am 2026-06-04 wurde `ISCC.exe` unter `C:\Program Files (x86)\Inno Setup 6\ISCC.exe` genutzt und `artifacts\installer\XDTBox_Setup_1.0.exe` erzeugt; die Setup-Datei ist noch nicht codesigniert. Die neue Doku `docs/INSTALLER_BUILD_ANLEITUNG.md` beschreibt Voraussetzungen, Build, Neuinstallation, Update, Deinstallation und manuelle Pruefschritte.
- Kundeninstaller gegen lokale Entwicklungsdaten gehaertet: Das Buildskript bereinigt vor jedem Neubau `artifacts\publish\XDTBox` und `artifacts\installer`, publisht danach frisch und bricht hart ab, wenn UserDefined-/persistierte Profile, lokale Schnittstellenprofile, AppSettings, Lizenzdateien, Geraetebild-Overrides, Baukasten-Templates, lokale Templatepakete, Backups, Diagnose-Logs, Testfixtures, Hersteller-Lizenztools, private Schluessel oder bekannte Marc-/Entwicklungspfade im Kundenpublish auftauchen. Das Setup packt weiterhin ausschliesslich den sauberen Publish-Ordner; BuiltIn-Werksvorlagen bleiben App-Bestandteil.
- Offizielle BuiltIn-Geraetebilder werden jetzt als WPF-App-Assets unter `XdtDeviceBridge.App\Assets\Devices` ausgeliefert. Alle BuiltIn-Geraeteprofile verweisen auf `pack://application:,,,/Assets/Devices/...`; lokale `DeviceImages` und `device-image-overrides.json` bleiben Kundendaten und werden weiter aus dem Installer ausgeschlossen. Die Bildaufloesung bleibt Override vor BuiltIn-App-Asset vor Platzhalter. Das Installer-Buildskript prueft vor dem Publish, dass die offiziellen BuiltIn-Assets im Repository vorhanden sind.
- Das BuiltIn `Generisches Dokumentgeraet` nutzt nun ein eigenes offizielles App-Asset `device-document-attachment-default.png`; die `Manuelle Dokumentauswahl` behaelt ihr separates Icon `device-manual-document-selection-default.png`. Beide Dateien sind Pflicht-Assets im Installerbuild.
- Installations-/Update-Datenpolitik vor einem spaeteren Installer festgelegt: `XdtBoxInstallationDataPolicy` trennt App-Bestandteile, BuiltIns, Kundendaten, temporaere Diagnose, externe Praxisordner, Herstellerdaten und private Hersteller-Schluessel. Die neue Doku `docs/INSTALLATION_UPDATE_DATENPOLITIK.md` beschreibt Update-Regeln, Backup-Abdeckung, Deinstallation Variante B und den Schutz von `%LocalAppData%\XdtDeviceBridge`, `license.xdtboxlic`, Baukasten-Templates, Geraetebild-Overrides und externen Praxisordnern. Es wurde kein Installer gebaut.
- Alter Tab `Profile & Templates` entfernt und die verbleibenden Aufgaben auf die neuen Zielbereiche verteilt: `XDT-Baukasten` bleibt Entwurf/Test/Vorschau, `Profilverwaltung` uebernimmt Uebersicht/Pflege/Wartung, und `Schnittstellenprofile` bleibt die konkrete Praxisanbindung. Der Baukasten initialisiert und arbeitet ohne Legacy-Tab-Abhaengigkeit.
- `Profilverwaltung` zur zentralen Pflegeflaeche ausgebaut: Die Ansicht zeigt AIS-, Geraete-, Export- und Schnittstellenprofile sowie lokale Templatepakete und Baukasten-Templates mit Suche, BuiltIn/UserDefined-Filter, Profiltypfilter, Details, Nutzungshinweisen und Wartungszeile. Neu verfuegbar sind Aktionen fuer neues AIS, neues Geraet, Geraet laden/Bildpflege, neues Exportprofil aus Vorlage, neues Schnittstellenprofil, Baukasten-Template erstellen, Templatepaket importieren und ausgewaehlte Templates/Pakete exportieren. Lokale Baukasten-Templates und Templatepakete koennen umbenannt/exportiert werden; BuiltIns bleiben geschuetzt, und verwendete Profile sowie aktive Schnittstellenprofile blockieren blindes Loeschen.
- XDT-Baukasten arbeitet eigenstaendig: eigene lokale Templatebibliothek (`Baukasten-Template laden` / `Konfiguration als Template speichern`), eigener Templatepaket-Importdialog mit Vorschau und UserDefined-Uebernahme sowie eigener RS232-Mitschnitt mit DTR/RTS und kleinem ASCII-Sendetest. Serielle Testdaten und allgemeine COM-Pruefung laufen zusaetzlich ueber `Schnittstellenprofile` / `RS232-Diagnose oeffnen`; der entfernte alte RS232-Testbereich wird nicht mehr benoetigt.
- NIDEK RT-6100 LAN-/MEM-200-Workflow gegen echte Herstellerdateien validiert: reale LM-7P-, ARK-1s- und RT-6100-Ophthalmology-XMLs liegen nun als Fixtures vor. Ein neuer RT-6100-Input-Source-Reader liest LM-XML als `LM_Base` und ARK-XML aus `ARMedian` als `REF_Base`; SR-/KM-/Bild-/Zusatzdaten und LM-Prismenwerte werden nicht blind exportiert, sondern als Warnung dokumentiert. Der RT-6100-Inputwriter erzeugt dokumentnaeheres UTF-16-XML mit vollstaendiger `Common`-/Patient-Struktur, ISO-Datum, `Measure Type="RT"`, Phoropter-Basisfeldern und Einheiten an Refraktions-/PD-/VD-/WorkingDistance-Tags. Der Baukasten erkennt LM-/ARK-Dateien als Quellen fuer `Export an Geraet`, waehrend echte RT-Rueckgabedateien weiterhin `Best -> 6228` und `Full -> 6227` erzeugen; Near-/Night-Varianten werden nicht als MEDISTAR-Hauptausgabe exportiert.
- NIDEK RT-3100 DirectWriter-Sendeschritt gegen zu fruehes Senden beziehungsweise Portschliessen abgesichert: Nach dem COM-Port-Oeffnen wartet der serielle Exchange nun kurz, damit DTR/RTS und RT-Eingang stabil sind; nach dem PC->RT-Writer-Frame wartet er zusaetzlich baudratenabhaengig, bis der Frame bei niedrigen 2400/7E2-Parametern rechnerisch auf der Leitung sein kann. Die Diagnose meldet Port-Bereitstellungswartezeit, Writer-Sendenachlauf und die Modemstatuswerte danach.
- NIDEK RT-2100/RT-3100/RT-5100 PC->RT-Sendeinhalt aus produktiven RT-Anbindungen nachgezogen: Neue Framevariante `ReferenceWithoutIdArAl` / `RT-Referenz ohne ID (AR/AL)` sendet AR- und LM-Bloecke ohne ID-Block, LM ADD als `AR`/`AL` und Abschluss `EB ET`. Diese Variante ist neuer BuiltIn-Default fuer die seriellen RT-Schnittstellenprofile und im Schnittstellenprofil sowie RT-Sendetest auswaehlbar. Die bisherige `RT-3100 Praxisvariante (getestet)` bleibt als Legacy-Testform erhalten. Die RS-Anforderung nutzt jetzt echte ASCII-Sternchen (`01 43 2A 2A 02 52 53 17 04`), passend zu den ausgewerteten produktiven RT-Anbindungen.
- NIDEK RT-3100 Praxis-Sendeframe als eigene Framevariante ergaenzt: `LegacyRt3100DirectFrame` / `RT-3100 Praxisvariante (getestet)` reproduziert den live angenommenen Direct-Writer-Frame exakt mit 107 Bytes, Legacy-ADD-Schreibweise `RA`/`LA` und ohne zusaetzliches `EB` direkt vor `ET`. Die Variante ist im Schnittstellenprofil und im RT-Sendetest als `NIDEK-RT Sendeinhalt` auswaehlbar; der RT-3100-BuiltIn-Default nutzt diese Praxisvariante, waehrend RT-2100/RT-5100 dokumentnah bleiben.
- NIDEK RT-2100/RT-3100/RT-5100 PC->RT-Writer gegen die Herstellerdokumentation nachgezogen und als dokumentnahe Alternative erhalten: LM-ADD kann als `AR`/`AL` gesendet werden, der Writer beendet Nutzdaten dokumentnah mit `EB ET`, und die Diagnose erkennt LM ADD passend. Fuer Live-Tests gibt es auswaehlbare Sendeinhalte (`Alle ausgewaehlten Werte`, `RT-3100 Praxisvariante (getestet)`, `Nur Autoref`, `Nur Lensmeter`, `Nur Lensmeter ohne ADD`, Varianten ohne ID und `Minimal rechts`) sowie die Testoption `CR nach EOT`. Der Sendeinhalt ist optional im Schnittstellenprofil speicherbar; Testvarianten bleiben explizite Diagnoseaktionen.
- NIDEK RT-3100 produktiven Button `An RT-3100 senden` nachgeschaerft: Der produktive `DirectWriterFrame` erzeugt und protokolliert den PC->RT-Writer-Frame jetzt vor dem COM-Port-Zugriff, sendet ueber denselben DirectWriter-Kern wie der erfolgreiche Diagnosemodus und wartet in diesem Sendeschritt nicht mehr auf eine Rueckgabe. Nach `Writer-Frame gesendet` wechselt XDTBox in den Pending-Status auf die spaetere RT-Rueckgabe; Fehler wie fehlende Auswahl, fehlender Writer-Frame oder COM-Schreibfehler werden sichtbar diagnostiziert.
- NIDEK RT-2100/RT-3100/RT-5100 RS232 Rueckgabe nach `DirectWriterFrame` produktiv angebunden: Das RT-Floating-Fenster trennt `COM-Port nur abhoeren` jetzt strikt als Diagnose ohne Export von `Rueckgabe abhoeren und verarbeiten`. Nach erfolgreichem Senden bleibt der Pending-Workflow mit AIS-Patientenkontext erhalten; die spaetere RT-Rueckgabe kann ueber den Profil-COM-Port bis `EOT` empfangen, mit dem gespeicherten Kontext gemappt und als MEDISTAR-XDT exportiert werden. Kurz-Timeouts direkt nach dem Senden werden als Wartestatus und nicht als Sendefehler angezeigt.
- NIDEK RT-2100/RT-3100/RT-5100 RS232 Sendemodus als Schnittstellenprofil-Einstellung ergaenzt. Die RT-BuiltIns verwenden nun `DirectWriterFrame` als praxisnahen Default: Nach Anwenderklick sendet XDTBox den PC->RT-Writer-Frame direkt, erwartet keine RS-/SD-Bestaetigung und wechselt danach in den Wartestatus auf die spaetere Phoropter-Rueckgabe. Alternativ bleiben `RS/SD-Handshake` und `RS senden, dann Writer ohne SD` speicherbar. Die Auswahl `NIDEK-RT Sendemodus` ist nur bei seriellen NIDEK-RT-Phoropterprofilen sichtbar; Diagnose-Testmodi im RT-Fenster bleiben davon getrennt und speichern nichts automatisch. Nach erfolgreichem Senden ohne sofortige Rueckgabe wird kein leeres XDT erzeugt und kein harter Sendefehler gemeldet.
- NIDEK RT-3100 RS232 PC->RT-Diagnose erweitert: Der LM-SCA-Writer sendet die Augenpraefixe jetzt als Leerzeichen + `R`/`L` statt als ASCII-Sternchen (`20 52`/`20 4C` statt `2A 52`/`2A 4C`). Die serielle Exchange-Diagnose protokolliert CTS/DSR/DCD/RI, DTR-Testsequenz, SD-Timeouts, optionale CR-Testanhaenge und Modemstatus an den relevanten Send-/Empfangspunkten. Im RT-Floating-Fenster gibt es zusaetzliche Sendetests `RS anfordern`, `DTR-Toggle + RS`, `Direkt Writer-Frame senden` und `RS + Writer ohne SD-Warten`; diese laufen nur nach explizitem Klick, erzeugen keinen XDT-Export und helfen beim Unterscheiden von Kabel/TX-Leitung, RT-Input-Mode und RS/SD-Handshake. Ein weiterer echter RT-3100-Nur-Abhoeren-Mitschnitt mit ADD/VA wurde als Fixture ergaenzt.
- NIDEK RT-3100 RS232 Live-Befund nachgezogen: Die RS-Anforderung fuer RT-2100/3100/5100 wird diagnostisch angezeigt und warnt beim RT-3100, wenn DTR deaktiviert ist. Ein erfolgreicher `COM-Port nur abhoeren`-Mitschnitt mit DTR aktiv wurde als weiteres RT-3100-Fixture uebernommen; Writer-Diagnosen melden LM ADD/PD-Bloecke passend zum erzeugten Frame.
- Serielle Live-Diagnose fuer den produktiven NIDEK RT-2100/RT-3100/RT-5100 RS232-Workflow ergaenzt: Das RT-Floating-Fenster zeigt nun COM-Port, Baudrate, Datenbits, Paritaet, Stopbits, Handshake, DTR/RTS, RS-Anforderung, erwartete/empfangene SD-Bestaetigung, PC->RT-Writer-Frame, Hexdump, sichtbare Steuerzeichen, Empfang bis EOT und Timeout-Hinweise. Ein sicherer Button `COM-Port nur abhoeren` nutzt die Profilparameter, sendet nichts, zeigt Rohbytes/Hexdump in der Diagnose und erzeugt keinen Export. RT-Type1-/Type2-Presets aktivieren DTR/RTS standardmaessig und die RS232-Test-/Schnittstellenprofil-UI macht DTR/RTS steuerbar.
- Produktiven seriellen Ablauf fuer NIDEK RT-2100 / RT-3100 / RT-5100 RS232 angebunden: Beim Start der Ueberwachung oeffnet kein RT-Fenster mehr; erst nach stabiler AIS-Patientendatei startet der Phoropter-Auswahldialog mit LM-/AR-Historienwerten. Nach ausdruecklichem Klick sendet XDTBox den PC->RT-Frame ueber den konfigurierten COM-Port, wartet auf `SD`, empfaengt die Rueckgabe bis `EOT` plus Stabilitaetswartezeit und verarbeitet den Rueckweg ueber das bestehende MEDISTAR-Mapping (`Final -> 6228`, `Subjective -> 6227`, keine `6330`). RS232-Scanhinweise sind Info statt Floating-Aktivitaet, und serielle RT-Schnittstellenprofile benoetigen keinen Geraete-Eingangsordner oder dateibasierten Geraete-Ausgabeordner.
- Ersten echten NIDEK RT-3100 RS232-Praxismitschnitt als reproduzierbare Hex-Fixture uebernommen und den `NidekRtSerialPhoropterParser` daran validiert. Der Parser unterstuetzt nun den bestaetigten line-based Frame `SH Header CR`, `STX @RT CR`, `STX Datenzeilen CR`, `EOT CR` ohne ETB zwischen den Datenzeilen, erkennt Final-R/L-SCA, ADD, VA, PD und WorkingDistance und erzeugt fuer den Mitschnitt ausschliesslich die MEDISTAR-`6228`-Final-Ausgabe. `6227`, `6330` und kuenstliche Trennzeilen werden nicht erzeugt; produktives Senden an echte RT-Geraete bleibt weiterhin offen.
- NIDEK RT-2100 / RT-3100 / RT-5100 als gemeinsame serielle RS232-Phoropterfamilie vorbereitet: Die drei Hersteller-PDFs wurden auf Steuerzeichen, Type1-/Type2-Parameter, RT->PC- und PC->RT-Blockformate ausgewertet. Neue BuiltIn-Geraete-, Export- und Schnittstellenprofile, RS232-Presets, `NidekRtSerialPhoropterParser`, `NidekRtSerialPhoropterOutputWriter`, Baukasten-Geraeteausgabe-Vorschau und selektive Templatepaket-Tests sind vorhanden. `Final` wird fuer MEDISTAR ueber `6228`, `Subjective` ueber `6227` abgebildet; `6330` und kuenstliche Trennzeilen werden nicht erzeugt. Produktives Senden an echte RT-2100/3100/5100-Geraete bleibt bis zu Praxis-Mitschnitten und Live-Abnahme bewusst deaktiviert.
- XDT-Baukasten-Ausgaben sind jetzt zeilenmodelliert: `Roh-XDT`, `Ansicht im AIS`, `Geraeteausgabe` und `Diagnose` zeigen Baukasten-Zeilennummern, ohne diese in den echten Ausgabetext aufzunehmen. Die Exportregel-Tabelle zeigt ebenfalls eine laufende Nummer, und die Auswahl einer Regel markiert die zugehoerige Ausgabezeile. DeviceOutput-Regelaenderungen fuer bidirektionale Geraete werden direkt in der Geraeteausgabe-Vorschau sichtbar; Regeln ohne erzeugte Ausgabe melden dies im Entwurfsstatus.
- XDT-Baukasten um getrennte Regelrichtungen fuer bidirektionale Workflows erweitert: `Konfiguration Exportregeln` kann nun zwischen `Export an AIS` und `Export an Geraet` umschalten. CV-5000/CV-5000S und NIDEK RT-6100 erhalten eigene DeviceOutput-Arbeitskopien, erweiterte Ausgabe-an-Geraet-Platzhalter mit Beispielwerten und eine reine Vorschaukopplung an die XML-Ausgabe; UserDefined-bidirektionale Geraete bleiben offen fuer eigene Regeln. Es werden weiterhin keine produktiven Geraete- oder AIS-Dateien geschrieben und BuiltIn-/Originalprofile bleiben unveraendert.
- XDT-Baukasten-Kompatibilitaetspruefung fuer Entwurfsarbeit entschaerft: ModelName-/Profilabweichungen blockieren die Baukasten-Vorschau nicht mehr, solange die Geraetedatei lesbar ist und Parser/Mapping Daten liefern. Der Baukasten zeigt stattdessen eine Warnung, haelt Rohdaten und Platzhalterwerte aus der geladenen Datei sichtbar und erweitert die Diagnose um Profil-Hersteller/-Modell, erkannte Dateiwerte, Parser, Exportprofil und Kompatibilitaetsstatus. Die produktive Verarbeitung bleibt davon unberuehrt und darf weiterhin streng pruefen.
- XDT-Baukasten-Praxistest-Korrekturen ergaenzt: `Templatepaket laden` gibt jetzt sichtbar im oberen Baukastenstatus und per Info-Dialog Rueckmeldung, `Ansicht im AIS` zeigt nur noch fachlich sichtbare Karteikartenzeilen ohne Patientendaten, Feldnummern oder technische Strukturueberschriften, und Geraete-/Messwert-Platzhalter werden getrennt nach Definition und aktuellem Messwert aus der geladenen Geraetedatei befuellt. Bei Geraeteprofilwechsel bleibt die Datei fuer bewusste Baukasten-Vergleiche erhalten; Abweichungen werden sichtbar gewarnt und in der Diagnose erklaert.
- XDT-Baukasten geraeteuebergreifend stabilisiert: Die Kompatibilitaetspruefung ist zentralisiert, BuiltIn-Profile pruefen Modell-Aliase wie `CV-5000`/`CV-5000S` gegen die geparste Datei, UserDefined-/Entwurfsprofile bleiben fuer neue Geraete permissiv und zeigen Parser-Messpfade dynamisch als Platzhalter. AR360, LM7, CV-5000/CV-5000S und weitere vorhandene XML-Fixtures sind testseitig als Baukasten-Referenz abgesichert.
- Erste read-only Bewertungslogik fuer einen spaeteren Aktivierungsassistenten importierter Schnittstellenprofile ergaenzt: Abhaengigkeiten, Ordner, XDT-Anhang-Konfiguration und Lizenzhinweise werden bewertet, ohne Profile zu aendern oder Verarbeitung zu starten.
- Erste UI-Pruefansicht fuer die Aktivierungsbewertung im Tab `Schnittstellenprofile` ergaenzt; sie zeigt Gesamtstatus, Aktivierbarkeit, Blocker, Warnungen und Hinweise, fuehrt aber weiterhin keine Aktivierung aus.
- Aktivierungspruefung um strukturierte Detailanzeigen fuer Ordnerpruefung und XDT-Anhang-Konfiguration erweitert; die Anzeige bleibt reine Vorschau ohne Datei-/Ordneroperationen.
- Vorbereitende Aktivierungsvorschau `Aktivierung vorbereiten` ergaenzt; sie fasst Status, Aktivierbarkeit, wichtigste Blocker und Warnungen zusammen und speichert weiterhin keine Aenderungen.
- Technische Guard-/Schutzschicht fuer eine schlanke spaetere V1-Aktivierung vorbereitet; sie prueft aktuelle Bewertung, BuiltIn/UserDefined-Schutz und blockiert `ReadyWithWarnings` fuer V1.
- Dialog `Aktivierung vorbereiten` von langer MessageBox auf ein scrollbares, resizebares Preview-Fenster umgestellt und auf V1 reduziert: Bewertung, technische Freigabe, Blocker, Warnungen, Hinweise und Sicherheitshinweis.
- Interface-/Model-Skelett fuer einen spaeteren `ActivationExecutor` ergaenzt; es beschreibt Request, Result, Preconditions und Statuswerte, bleibt aber ohne produktive Implementierung, UI-Anbindung, Speicherung oder Aktivierungswirkung.
- Defensiven `InterfaceProfileActivationExecutorStub` ergaenzt; er bewertet V1-Preconditions und liefert Statuswerte wie `ReadyButNotExecuted`, `Blocked` oder `NotImplemented`, fuehrt aber keine Aktivierung, Speicherung, Datei-/Ordneroperation oder Verarbeitung aus.
- `ActivationExecutor`-Request und -Result fuer eine spaetere Backend-Aktivierung auf V1-Kontext gestrafft: Zielprofil-ID/-Name, OperationMode, Preview-Kontext sowie Result-Flags fuer frisches Laden, sichere UserDefined-Speicherung und finale Bewertung.
- Executor-nahe Store-/Loader-Abstraktion fuer spaeteres frisches Laden und sichere UserDefined-Speicherung vorbereitet; der aktuelle Store-Stub arbeitet nur in-memory, blockiert BuiltIn/Nicht-UserDefined und speichert weiterhin nichts.
- `InterfaceProfileActivationProfileCatalogStore` als ValidateOnly-Adapter gegen `ProfileCatalogService`/`AppDataPaths` ergaenzt; frisches Laden ist moeglich, Save bleibt DryRun ohne `SaveInterfaceProfileDefinition`, ohne JSON-Schreibzugriff und ohne Profilmutation.
- `InterfaceProfileActivationExecutorStub` kann im `ValidateOnly`-Modus optional einen `IInterfaceProfileActivationProfileStore` nutzen; frisches Laden, BuiltIn/UserDefined-Pruefung und Save-DryRun werden ausgewiesen, bleiben aber ohne Aktivierung, ohne Speicherung und ohne UI-Anbindung.
- `InterfaceProfileActivationExecutorStub` bereitet finale Bewertung als nicht-produktive `ValidateOnly`-Simulation vor: Evaluation und Guard koennen optional neu erzeugt werden; fehlen Services, werden MissingCapabilities gemeldet, und Save bleibt DryRun ohne Profilmutation.
- Reproduzierbaren Testweg fuer das MEDISTAR + NIDEK ARK1S Referenz-Templatepaket ergaenzt: `TemplatePackageExporter` erzeugt die ZIP temporaer, `TemplatePackageImporter` liest sie wieder ein, und der sichere Importfluss schuetzt BuiltIns und importiert nur inaktive UserDefined-Kopien.
- Selektiven Templatepaket-Export ergaenzt: Im Tab `Profile & Templates` wird ein Schnittstellenprofil als Paketbasis gewaehlt; exportiert werden nur dieses Schnittstellenprofil und die benoetigten AIS-, Geraete- und Exportprofile.
- Schlanke Bedienfunktionen fuer Exportprofil-Wartung ergaenzt: UserDefined-Exportprofile koennen geloescht werden, wenn kein Schnittstellenprofil sie verwendet; Exportregeln koennen nur aus UserDefined-Exportprofilen entfernt werden. BuiltIns bleiben gesperrt.
- NIDEK AR360 / AR-360A als testgestuetzten Auto-Refraktometer-Kandidaten ergaenzt: BuiltIn-Geraeteprofil, MEDISTAR-Exportprofil, Schnittstellenprofil, ARMedian-XML-Fixtures, UTF-16-Lesetest und selektiver Templatepaket-Test.
- Praktische MEDISTAR-Validierung fuer NIDEK AR360 dokumentiert und testseitig abgesichert: `8402 = AR360`, rechte/linke `6228`-Auto-Refraktor-Zeilen, ARMedian-Achse `172` und `VD= 12.00 mm`.
- AR360-Referenzpakettest analog zu ARK1S ergaenzt: selektiver Export, ZIP-Struktur, Importvorschau, DryRun, sichere UserDefined-Kopie, inaktive Schnittstellenprofile und deaktiviertes `IsAttachmentProcessingEnabled` werden reproduzierbar geprueft.
- Erste Grundlage fuer abdockbare Geraeteanbindungsfenster im Tab `Verarbeitung` ergaenzt: Monitoring-Karten koennen manuell abgedockt und wieder angedockt werden; Pin und Positionsmerken sind pro Schnittstellenprofil als UI-Zustand vorbereitet.
- Erste Stufe fuer automatisches Abdocken bei Monitoring-Aktivitaet ergaenzt: AIS-/Geraete-/Anhang-/Paket- und Verarbeitungsevents oeffnen nur das betroffene Geraetefenster und nutzen einen kurzen Cooldown gegen Event-Spam.
- Akustisches Signal bei Geraetedatei-Eingang ergaenzt: `04_praxis_terminal_signal.wav` wird als App-Content ausgeliefert, pro Schnittstellenprofil per Cooldown begrenzt und bei fehlender/defekter Sounddatei defensiv abgefangen.
- Automatisches Zurueckandocken nach terminalem Monitoring-Abschluss ergaenzt: automatisch geoeffnete, nicht gepinnte Geraetefenster docken nach 5 Sekunden Restlaufzeit wieder an; neue Aktivitaet, Pin oder manuelles Andocken brechen den Countdown pro Schnittstellenprofil ab.
- Systray-Grundfunktion ergaenzt: Das Hauptfenster kann per Minimieren oder `X` in den Infobereich ausgeblendet werden, waehrend Verarbeitung und Floating-Geraetefenster weiterlaufen; Doppelklick beziehungsweise Kontextmenue `Oeffnen` stellt es wieder her, `Beenden` schliesst die App bewusst.
- Sicherer Reset fuer Geraeteanbindungsfenster angepasst: `↺` verwirft den aktuellen Vorgang fuer genau ein Schnittstellenprofil und leert nach Sicherheitsabfrage nur dessen AIS-, Geraete- und optionalen XDT-Anhang-Eingangsordner top-level; Export-, Archiv- und Fehlerordner sowie Unterordner bleiben unangetastet.
- Floating-Geraeteanbindungsfenster leicht verbreitert und die Symbolleiste oben rechts fix horizontal gesetzt, damit `🗗 ↺ 📌 🔝` nicht umbrechen.
- CV-5000/CV-5000S-Monitoring als grafischen Pilot ergaenzt: Die CV-5000-Karte und das Floating-Geraetefenster nutzen statt Radar ein Geraetebild, einen linken Infoblock, eine pulsierende Statuskugel mit gruenem Dateieingangs-Flash und eine mittig platzierte Intervallsteuerung. `Letzter Scan` und `Automatik` stehen fuer diesen Pilot im Detailbereich; andere Geraetekarten bleiben beim bisherigen Radar.
- CV-5000/CV-5000S-Pilotdarstellung nach Sichtpruefung nachgeschaerft: kompakteres Fenster, groesseres Geraetebild, keine doppelte obere Titelzeile, keine separate Statuszeile im Infoblock und Statuskugel-Puls nur bei laufender Ueberwachung.
- CV-5000/CV-5000S-Pilotdarstellung final nachgeschaerft: Das Geraetebild nutzt im gleichen Fenster mehr Raum, die Intervallsteuerung ist linksbuendig gesetzt, und laufende Ueberwachung zeigt in leeren Eingangskacheln `wartet` statt faelschlich `gestoppt`.
- CV-5000/CV-5000S-Pilotdarstellung als Standardlayout fuer alle Monitoring-/Geraetefenster vorbereitet: Alle aktiven Geraete nutzen nun Infoblock, optionales Geraetebild beziehungsweise Platzhalter, Statuskugel mit Puls/Flash und linksbuendige Intervallsteuerung; das alte Radar bleibt nur als technischer Fallback im XAML.
- Optionales `Geraetebild` fuer neue UserDefined-Geraeteprofile vorbereitet. Das Bildfeld ist rein optional, speichert nur einen Pfad und dient als Grundlage fuer spaetere grafische Geraetefenster.
- GUI-Funktion `Geraet laden` im Tab `Profile & Templates` ergaenzt: bestehende BuiltIn- und UserDefined-Geraete koennen angezeigt werden, Geraetebilder werden lokal unter AppData `DeviceImages` kopiert, BuiltIn-Bildwechsel laufen ueber `device-image-overrides.json`, und fehlende Bilder fallen weiter auf Platzhalter beziehungsweise BuiltIn-Asset zurueck.
- Bildaufloesung und Monitoring-Text fuer Geraetefenster nach Sichtpruefung korrigiert: `Geraet laden` nutzt dieselbe Override-/BuiltIn-Asset-Fallbacklogik wie die Monitoring-Karte, und lange interne Geraetetypen werden im Hauptbereich ueber kurze Displaylabels wie `Keratorefraktometer`, `Tonometer` oder `Autorefraktor` dargestellt, ohne die fachlichen Profile zu veraendern.
- Monitoring-/Geraetefenster-Layout verbreitert und lesbarer gemacht: Standardkarten nutzen 576 px statt 480 px, Floating-Fenster 744 px statt 620 px Startbreite, Text- und Bildspalte liegen naeher zusammen, und fuer sehr lange Anzeigenamen ist ein Text-ueber-Bild-Fallback vorbereitet.
- Monitoring-/Geraetefenster-Kacheln und Statuskugel nach Sichtpruefung angepasst: Eingangskacheln wachsen bei laengeren Bezeichnungen moderat mit, gestoppte Ueberwachung zeigt eine rote statische Kugel, laufende Ueberwachung eine gruen pulsierende Kugel und Dateieingang einen schnellen weiss/gelb/weiss-Blitz.
- UI-Ueberlagerung im Tab `Schnittstellenprofile` unterhalb der Ordnerbereinigung behoben.
- Schlanke V1-Profilanlage im Tab `Profile & Templates` ergaenzt: `Neues AIS anlegen` und `Neues Geraet anlegen` oeffnen einfache Dialoge und speichern nur UserDefined-Profile; `Neues Exportprofil anlegen` bereitet den vorhandenen Exportregel-Entwurf als UserDefined-Kopie vor. BuiltIns bleiben geschuetzt, es erfolgt keine Aktivierung und keine Verarbeitung.
- Button `Neues Schnittstellenprofil anlegen` im Tab `Schnittstellenprofile` funktionsfaehig gemacht: Der neue Dialog waehlt Profilname, AIS-, Geraete- und Exportprofil, speichert ein inaktives UserDefined-Schnittstellenprofil mit leeren Ordnern und startet keine Verarbeitung. BuiltIn-Profile bleiben unveraendert.
- Visuelles Speichern-Feedback im Tab `Schnittstellenprofile` ergaenzt: Nach erfolgreichem Speichern faerbt sich der Speichern-Button kurz dezent gruenlich und zeigt `Gespeichert`; bei Fehlern erscheint kein Erfolgsfeedback.
- Ordner-Vorbereitung im Tab `Schnittstellenprofile` ergaenzt: `Ordner Default` fuellt Standardpfade unter `C:\XDTBox\<Geraetename>` in die sichtbaren Pfadfelder, `Ordner anlegen` erstellt nur die aktuell eingetragenen Ordner und meldet Erfolg beziehungsweise konkrete Pfad-/Berechtigungsfehler.
- XDTBox-Geraeteanbindung um eine vorbereitete Eingangsquellen-Modellierung erweitert: `NetworkLan` beschreibt den bisherigen Datei-/UNC-Workflow ueber Geraete-Eingangsordner, `SerialRs232` ergaenzt COM-Port-Profile mit Baudrate, Datenbits, Stoppbits, Paritaet, Flusskontrolle und bidirektionaler Option. Alte Profile bleiben rueckwaertskompatibel LAN/File-Import.
- RS232-/COM-Port-Testfunktion im Tab `Profile & Templates` ergaenzt: COM-Ports koennen aktualisiert, Kommunikationsparameter gesetzt, ein zeitlich begrenzter Mitschnitt gestartet/gestoppt, optional Text gesendet und Rohtext/Hexdump/Bytes/Status angezeigt werden. Fehlende oder belegte Ports fuehren zu verstaendlichen Statusmeldungen statt App-Abbruch.
- Generische NIDEK-RS232-Protokollschicht vorbereitet: `NidekRs232FrameReader` liest SOH/STX/ETB/EOT-Frames inklusive optionaler NCP10-Checksumme, `NidekRs232CommandBuilder` erzeugt sichere RS/SD/RD/RSD/CL-Kommandos, und `NidekRs232PayloadParser` erkennt erste LM-/NT-/PM-Rohdaten als Messwertkandidaten fuer `6228`, `6205` beziehungsweise `6220`, ohne produktiven XDT-Export aus seriellen Rohdaten zu aktivieren.
- RS232-Testbereich um NIDEK-Auswertung erweitert: Der Anwender kann `Raw` oder `NIDEK RS232` waehlen, NCP10 beziehungsweise vorbereiteten NIDEK-RS-SD-Modus markieren und empfangene oder eingefuegte Rohdaten/Hexdumps in Frames, Header, Segmente, Checksumme, Modell und Messwertkandidaten zerlegen lassen. Unbekannte Segmente bleiben roh erhalten.
- Tab `Schnittstellenprofile` unterscheidet die Geraetequelle: LAN-/Dateigeraete zeigen weiter `Geraetedatei an XDTBox`, RS232-Profile zeigen stattdessen `Serielle Geraetekommunikation / COM-Port`; AIS-Patientenordner, Ergebnisordner, Archiv und Fehler bleiben auch bei RS232 konfigurierbar.
- Produktivplan fuer die XDTBox-Lizenzierung konkretisiert: V1 lizenziert ausschliesslich die Anzahl aktiver Geraeteanbindungen (`MaxActiveDeviceConnections`), LAN/UNC, `SerialRs232`, Dokumentanhang, Profile/Templates und Testbereiche sind keine separaten Module. `LicenseEnvelope`, `LicensePayload` und eine nicht produktiv angeschlossene V1-Policy-Bewertung bereiten signierte Offline-Lizenzen, 7-Tage-Karenz und Blockadeentscheidungen nach ungueltiger Signatur beziehungsweise Karenzende vor. Der Tab `Lizenz` zeigt den Hardwaretausch-Hinweis; es wurde keine harte Lizenzsperre aktiviert.
- Echte V1-Signaturpruefung und `.xdtboxlic`-Import vorbereitet: `LicenseEnvelopeReader`, `LicenseSignatureVerifier`, `LicensePublicKeyProvider` und `LicenseImportService` pruefen RSA-PSS/SHA-256-Signaturen ueber die Payload-Bytes, bewerten ProductCode/InstallationId/Ablauf/Karenz/`MaxActiveDeviceConnections` und zeigen klare Statusmeldungen im Lizenz-Tab. Legacy-JSON-Lizenzen bleiben als unsignierter Uebergang lesbar; eine harte produktive Sperre wurde nicht aktiviert.
- Internes Herstellerwerkzeug `XdtBox.LicenseIssuer` ergaenzt: Das Console-Tool erzeugt aus Lizenzanforderung oder InstallationId signierte `.xdtboxlic`-Dateien mit RSA-PSS/SHA-256, laedt den privaten Schluessel nur aus einem externen PEM-Pfad und wurde framework-dependent als `XdtBox.LicenseIssuer.exe` nach `C:\XDTBox\Lizenzaktivierung` veroeffentlicht. Es wurde kein privater Schluessel ins Repository oder in die App aufgenommen und keine produktive Geraeteblockade aktiviert.
- `XdtBox.LicenseIssuer.exe` fuer Doppelklick/Start ohne Parameter freundlicher gemacht: Ohne Argumente zeigt das Tool Fehlerhinweis, Hilfe, Beispielaufruf und Private-Key-Sicherheitshinweis, wartet in interaktiver Konsole auf Tastendruck und liefert ExitCode `1`; `--help` bleibt ein normaler nicht pausierender CLI-Aufruf. Eine Hilfe-Batchdatei wird mitveroeffentlicht.
- Grafische Hersteller-Lizenzverwaltung `XdtBox.LicenseManager` ergaenzt: Die WPF-App liest XDTBox-Lizenzanfragen mit Kundendaten und dokumentierten Geraeteanbindungen, erzeugt signierte `.xdtboxlic`-Dateien ueber die bestehende RSA-PSS/SHA-256-Issuer-Logik, fuehrt eine lokale JSON-Historie ausgestellter Lizenzen unter `C:\XDTBox\Lizenzaktivierung\data\license-history.json` und speichert Hersteller-Einstellungen unter `license-manager-settings.json`. Private Schluessel bleiben externe Dateien und werden nicht in App oder Repository aufgenommen.
- Lizenzanforderung der Kunden-App erweitert: Im Tab `Lizenz` koennen Praxis-/Firmendaten fuer die Anfrage gepflegt werden; exportierte Lizenzanfragen enthalten zusaetzlich dokumentierende Geraeteanbindungsnamen, DeviceProfileId und ConnectionKind. Lizenzbindend bleibt ausschliesslich `MaxActiveDeviceConnections`.
- `XDTBox.ico` als offizielles Programmicon uebernommen: Kunden-App, Systray-NotifyIcon, grafisches Hersteller-Lizenztool und optional auch das CLI-Lizenztool verwenden dasselbe `.ico`-Asset.
- XDTBox-Branding mit Logo/Schriftzug integriert: Kunden-App und grafisches Hersteller-Lizenztool nutzen den neuen Markenheader, zentrale XDTBox-Farbresources und modernisierte Grundstyles; die abgenommenen Geraeteanbindungsfenster bleiben strukturell unveraendert.
- Produktiv-UI der Kunden-App weiter vereinfacht: Der Tab `Verarbeitung` entfernt den Einzelscan-Button und die globale Auto-Verarbeiten-Checkbox; passende Dateipaare werden bei laufender Ueberwachung immer automatisch verarbeitet, und die Ueberwachung startet beim Oeffnen der XDTBox-App einmalig automatisch.
- Tab `Sicherung/Umzug` ergaenzt: XDTBox erstellt `.xdtboxbackup`-ZIP-Sicherungen mit Manifest fuer Profile, lokale Geraetebilder, Bild-Overrides, Lizenz-Kundendaten, UI-Komfortdaten und optional importierte `.xdtboxlic`; Restore prueft Manifest/ProductCode, verlangt gestoppte Ueberwachung und sichert keine Patientendaten, Messdateien, Export-, Archiv- oder Fehlerordner-Inhalte.
- Hilfe/Info im XDTBox-Header ergaenzt: Ein `?`-Menue oeffnet ein lokales Hilfe-Center mit Anwenderanleitungen und einen Info-Dialog mit Version und Herstellerdaten von Technik-Apparat M.Kurze.
- UI-Nachschaerfung fuer die Kunden-App: Hilfe/Info wurde in die Tab-Zeile verlegt, rechts daneben oeffnet ein Zahnrad die App-Einstellungen fuer Autostart-Ueberwachung sowie Start-/Schliessverhalten im Systray. Die Bereiche in `Profile & Templates` und `Schnittstellenprofile` sind einklappbar und standardmaessig ausgeklappt, und die Vorschau `Pruefung vor Aktivierung` nutzt getrennte Status- und Aktionszeilen gegen Textueberlagerungen.
- Dialoge fuer `Neues AIS anlegen` und `Neues Geraet anlegen` verstaendlicher gemacht: kurze Hilfetexte erklaeren Profilname, AIS-System, Codierung, Hersteller, Modell, Geraetetyp und Parserbasis; `Generisch` wird als anderer AIS-Kontext beziehungsweise interner Fallback erklaert.
- Sichere Umbenennung fuer UserDefined-Profile ergaenzt: AIS-, Geraete-, Export- und Schnittstellenprofile koennen ihren sichtbaren Namen aendern, waehrend technische IDs, Referenzen, Ordner, Exportregeln und XDT-Anhang-Einstellungen unveraendert bleiben; BuiltIns bleiben gesperrt.
- NIDEK RT-6100 als bidirektionalen LAN-/MEM-200-Phoropter-Kandidaten ergaenzt: BuiltIn-Geraeteprofil, Exportprofil, Schnittstellenprofil, RT-6100-Import-XML-Writer fuer LM_Base/REF_Base, Rueckgabeparser fuer NIDEK_RT-XML und selektiver Templatepaket-Test. `Best` wird vollstaendig ueber `6228`, `Full` vollstaendig ueber `6227` ausgegeben; `6330` und kuenstliche Trennzeilen werden nicht erzeugt. Die bereitgestellte OCR-XML ist nicht wohlgeformt und wird als Diagnosefall behandelt; die Erfolgsfixtures sind aus der PDF-Struktur abgeleitet.
- Baukasten-Testvorschau im Tab `Profile & Templates` profilabhaengig korrigiert: Statt fest auf NIDEK ARK1S zu lesen, verwendet die Vorschau nun das ausgewaehlte Exportprofil mit passendem Geraete-/Schnittstellenprofil. Dadurch nutzt der manuelle RT-6100-Test denselben MEDISTAR-Historien-Fallback fuer Phoropter-AIS-Dateien wie das Live-Geraetefenster, ohne Dateien zu schreiben, zu archivieren oder die produktive Verarbeitung zu veraendern.
- Neuer eigenstaendiger Tab `XDT-Baukasten` ergaenzt und nachgeschaerft: Die Arbeitsflaeche fuehrt AIS-, Geraete- und Exportprofil, Testdaten, Rohdatenanzeigen, reine In-Memory-Verarbeitungsvorschau, vier Ergebnisansichten (`Roh-XDT`, `Ansicht im AIS`, `Geraeteausgabe`, `Diagnose`), Exportregel-Arbeitskopie und interaktive Platzhalter mit Beispielwerten zusammen. Exportregeln koennen in der Arbeitskopie per Plus/Muelltonne ergaenzt oder entfernt werden, feste Notizen ohne `SourcePath` sind erlaubt, Undo haelt die letzten Baukasten-Aenderungen vor, und Windows-1252-/UTF-Rohdaten werden encoding-sicher angezeigt. Der alte Tab `Profile & Templates` bleibt uebergangsweise erhalten; der neue Baukasten startet keine produktive Verarbeitung, schreibt keine AIS-Exportdateien und ist UI-seitig nicht an die alten Baukasten-Controls gekoppelt.
- NIDEK LM7 / LM-7P als praktisch nutzbaren Lensmeter-Kandidaten ergaenzt: echte `NIDEK LM7.xml`-Fixture, `Sphare`/`Sphere`-Alias, berechnete MEDISTAR-`6228`-Lensmeterzeilen, BuiltIn-Schnittstellenprofil und selektiver LM7-Templatepaket-Test.
- `NIDEK_LM_Stylesheet.xsl` wird als Anzeige-Stylesheet ignoriert und nicht als Geraetedatei klassifiziert.
- Praktische MEDISTAR-Validierung fuer NIDEK LM7 dokumentiert: AIS- und echte LM7-XML-Datei wurden verarbeitet, MEDISTAR uebernimmt `8402` aus AIS sowie die rechten/linken `6228`-Lensmeterzeilen ohne leere Prisma-/PD-/ADD-Fragmente.
- NIDEK NT530P / NT-530P als direkt nutzbaren MEDISTAR-Kandidaten ergaenzt: echte UTF-16-XML-Fixture, Parserwerte fuer Tonometrie, korrigierten IOP und Pachymetrie, berechnete MEDISTAR-Zeilen fuer `6220` und `6205`, BuiltIn-Schnittstellenprofil und selektiver NT530P-Templatepaket-Test. JPG-Dateien werden nicht als Messwertdateien verarbeitet und bleiben fuer spaetere XDT-Anhang-Validierung relevant.
- TOPCON CL-300 als erstes vollstaendiges TOPCON-Geraet ergaenzt: echte CL-300-XML-Fixtures `Serial0001` und `Serial1521`, Namespace-tolerante LM-Erkennung, berechnete MEDISTAR-`6228`-Lensmeterzeilen mit ADD, PD und signierten H/V-Prismenkomponenten, BuiltIn-Geraete-/Export-/Schnittstellenprofil und selektiver Templatepaket-Kandidat.
- Praktische MEDISTAR-Validierung fuer TOPCON CL-300 dokumentiert: Serial0001 und Serial1521 wurden als Lensmeter-XDT-Rueckgabe uebernommen, `8402` kommt aus AIS, `6228` enthaelt die rechten/linken Lensmeter-Zeilen inklusive ADD, PD und konservativer H/V-Prismenkomponenten ohne OUT/IN/UP/DOWN-Raten.
- TOPCON Solos als Lensmeter-Kandidat ergaenzt: echte `SolosExportSample.xml`-Fixture, eigene BuiltIn-Geraete-/Export-/Schnittstellenprofile, TOPCON-LM-XML-Erkennung ueber `ModelName = SOLOS`, MEDISTAR-`6228`-Lensmeterzeilen mit PD und signierten H-Prismen, selektiver Templatepaket-Test sowie Dokumentation. Transmission und PDF-Berichte bleiben ohne echte gefuellte Fixtures bewusst offen.
- TOPCON CT-800A als Non-Contact-Tonometer-Kandidat ergaenzt: drei echte CT-800A-XML-Fixtures, eigene BuiltIn-Geraete-/Export-/Schnittstellenprofile, TOPCON-TM-XML-Erkennung ueber `ModelName = CT-800A`, MEDISTAR-`6205`-Tonometriezeilen, vollstaendige CorrectedIOP/CCT-Detailausgabe nur bei verwertbaren per-eye-Daten und keine kuenstliche `6220`-Pachymetrie.
- TOPCON KR-1 als Keratorefraktometer-Kandidat ergaenzt: echte Shift-JIS-XML-Fixture `Serial0001`, eigenes BuiltIn-Geraete-/Export-/Schnittstellenprofil, TOPCON-REF-Erkennung ueber `ModelName = KR-1`, REF-Medianwerte ueber MEDISTAR-`6228`, selektiver Templatepaket-Test sowie Dokumentation. KM/KRT/periphere KRT-Ausgabe bleibt ohne echte KM/KRT-Fixture bewusst offen.
- TOPCON KR-800S als vollstaendige TOPCON-Mehruntersuchungsanbindung ergaenzt: echte Shift-JIS-XML-Fixtures `Serial0036` und `Serial0426`, namespace-tolerante REF/KM/SBJ-Erkennung, berechnete MEDISTAR-Zeilen fuer `6228` Autorefraktion, `6221` Keratometrie und konservative `6227`-SBJ-Ausgabe, BuiltIn-Geraete-/Export-/Schnittstellenprofil und selektiver Templatepaket-Kandidat.
- TOPCON TRK-2P als vollstaendige TOPCON-Mehruntersuchungsanbindung ergaenzt: echte XML-Fixtures `Serial0001` und `Serial0135`, namespace-tolerante REF/KM/TM/CCT-Erkennung, berechnete MEDISTAR-Zeilen fuer `6228` Autorefraktion, `6221` Keratometrie, `6220` Pachymetrie aus CCT und `6205` Tonometrie, optionaler SBJ-`6227`-Pfad, BuiltIn-Geraete-/Export-/Schnittstellenprofil und selektiver Templatepaket-Kandidat.
- TOPCON CT-1P als Tonometer-/Pachymeter-Kandidat ergaenzt: echte Serial0214-XML-Fixture mit `nsCommon`/`nsTM`, namespace-tolerante TM- und CorrectedIOP/CCT-Erkennung, `6205`-Tonometrie im NT530P/TRK2P-Mehrzeilenformat, `6220`-Pachymetrie, BuiltIn-Geraete-/Export-/Schnittstellenprofil und selektiver Templatepaket-Kandidat. Das Geraet misst fachlich vollautomatisch beidseitig Tonometrie plus Pachymetrie; unvollstaendige per-eye-CorrectedIOP-Bloecke werden ohne leere Fragmente ausgelassen.
- Praktische MEDISTAR-Validierung fuer TOPCON CT-1P dokumentiert: Serial0214 wurde als Tonometrie-/Pachymetrie-XDT-Rueckgabe uebernommen, `8402 = CT-1P` kommt aus AIS, `6220` enthaelt die rechte Pachymetrie, `6205` enthaelt Tonometrie inklusive linker IOP-Zusammenfassung, und der unvollstaendige linke CorrectedIOP/CCT-Block erzeugt keine leeren Fragmente.
- TOPCON CV-5000 / CV-5000S als ersten bidirektionalen Phoropter-Kandidaten ergaenzt: MEDISTAR-Historienparser fuer Karteikartenzeilen, CV-5000-Import-XML-Writer, CV-5000-Rueckgabeparser, fachlich getrennte Phoropter-Rueckgabe, BuiltIn-Geraete-/Export-/Schnittstellenprofil und selektiver Templatepaket-Test. Das Geraeteprofil markiert nur die bidirektionale Faehigkeit; die konkrete Ausgabe an das Geraet wird im Schnittstellenprofil gepflegt. Bei CV-5000/CV-5000S ist der Bereich `Ausgabe an Geraet` sichtbar und `XDT-Anhaenge fuer AIS` bewusst ausgeblendet. Die praktische MEDISTAR-/CV-5000-Livevalidierung steht noch aus.
- XDT-Mehrfachanhaenge im Automatiklauf ergaenzt: mehrere stabile unterstuetzte Anhangdateien werden sortiert einzeln per bestehender Copy/Move- und Kollisionslogik uebertragen und erzeugen je Datei eine eigene `6302`/`6303`/optional `6304`/`6305`-Linkfeldgruppe.
- AttachmentOnly-/Dokumentgeraete-V1 ergaenzt: Das BuiltIn-Profil `MEDISTAR + Dokumentanhang` verarbeitet AIS-Datei plus eine oder mehrere stabile Dokument-/Anhangdateien ohne Messwertparser und nutzt fuer jede Datei die bestehenden `6302`/`6303`/`6304`/`6305`-Linkfelder. XML, MP4, MP3 und WAV werden in diesem Modus nur als Anhaenge behandelt.
- AttachmentOnly-Abschlusslogik ergaenzt: Dokumentgeraete koennen profilbezogen entweder nach einer Wartezeit nach der letzten stabilen Datei automatisch uebertragen oder im Dialog `Dokumente uebertragen` manuell bestaetigt werden. Neue Dateien innerhalb der Wartezeit starten den Countdown neu; Drag-&-Drop fuer automatisch liefernde Dokumentgeraete und Vorschaukachel-Galerien bleiben weiterhin offen.
- `MEDISTAR + Manuelle Dokumentuebergabe` als eigenes BuiltIn-Schnittstellenprofil ergaenzt: AIS startet den Dialog `Dokumente uebertragen` mit leerer Liste, Dateien koennen per Drag-&-Drop oder Dateiauswahl hinzugefuegt werden, und jede Datei wird erst nach `Uebertragen` ueber eigene `6302`/`6303`/`6304`/`6305`-Linkfelder exportiert.
- Tests fuer die Aktivierungsbewertung importierter Schnittstellenprofile ergaenzt, inklusive fehlender Abhaengigkeiten, fehlender Pflichtordner, BuiltIn-Schutz, optional deaktivierter XDT-Anhang-Automatik und lizenzpflichtiger Profile.

### Behoben

- Backup-Restore robuster gemacht: Verwendete Geraetebilder werden beim Wiederherstellen als Warnung behandelt, andere Konfigurationsbereiche werden wiederhergestellt, und Bilddateien werden in der App ohne dauerhaften Dateilock geladen.
- Lizenz-Refresh nach `.xdtboxlic`-Import korrigiert: `Karenzzeiten aktualisieren` verwendet jetzt dieselbe lokale Lizenzquelle wie der Lizenz-Tab, bevorzugt gueltige `license.xdtboxlic` vor Legacy-`license.json` und setzt `MaxActiveDeviceConnections` nicht mehr auf `0` zurueck. Erfolgreicher signierter Import zeigt zusaetzlich die freundliche Rueckmeldung zur lizenzierten Geraeteanzahl.
- CV-5000/CV-5000S-Rueckweg repariert: MEDISTAR-Historien-AIS-Dateien mit Karteikartenzeilen werden beim Phoropter-Rueckexport gezielt tolerant ueber den CV-5000-Historienparser als Patientenkontext gelesen, sodass die `6228`-Rueckgabe nicht mehr mit der pauschalen Meldung `AIS-Datei konnte nicht fehlerfrei gelesen werden` abbricht. Die `CVImport.xml`-Erzeugung bleibt Phase 1 und markiert den Workflow nicht terminal; der Abschluss erfolgt erst nach erfolgreicher Phoropter-Rueckgabe. Fehlerdetails fuer AIS-/GDT-Leseprobleme werden genauer protokolliert.
- CV-5000/CV-5000S-Rueckgabe nach praktischem MEDISTAR-Importtest angepasst: `6330` wird fuer CV-5000 nicht mehr verwendet. `Prescription` wird vollstaendig ueber `6228` ausgegeben, `Full Correction` vollstaendig ueber `6227`; die alte `6228 --`-Trennzeile bleibt entfallen und es wurde keine generische `6330`-Automatik eingefuehrt.
- CV-5000/CV-5000S-Auswahlfenster im echten Scanpfad angebunden: Bei aktivierter `Ausgabe an Geraet` und gesetztem Ausgabeordner oeffnet nach AIS-Datei-Eingang das Fenster `Werte an Phoropter uebergeben`, gruppiert Historienwerte, waehlt neueste `V0`/`V1`/`V2` vor und schreibt `CVImport.xml` ueber die Schnittstellenprofil-Konfiguration. Normale Geraete triggern diesen Dialog nicht.
- CV-5000/CV-5000S-Auswahlfenster praxisnaeher gemacht: Das Fenster startet standardmaessig TopMost mit dem bestehenden `🔝`-Schalter und bietet `Nichts senden` als eigenen fachlichen Pfad. Dabei wird keine `CVImport.xml` erzeugt, kein Fehler gesetzt und weiter auf die Phoropter-Rueckgabe gewartet; `Abbrechen` bleibt davon getrennt.
- Dateiversions-/AlreadyProcessed-/Duplicate-Sperren aus der automatischen Verarbeitung entfernt: Aktuell im konfigurierten Eingangsordner liegende stabile, lesbare und fachlich passende AIS-/Geraetedateien werden verarbeitet, unabhaengig von frueher gesehenem Pfad, Dateiname, Fingerprint oder Zeitstempel. Dadurch funktionieren CV-5000/CV-5000S-Folgezyklen mit gleicher Dateinamenstruktur wie `Patient.gdt` ohne Reset.
- CV-5000/CV-5000S-Zweiphasenlauf vereinfacht: Nach Phase 1 wartet die App auf eine stabile CV-5000/CV-5000S-Rueckgabedatei im Geraeteordner und startet bei einem fachlich vollstaendigen Paar den finalen MEDISTAR-Export ohne Baseline- oder Zeitstempelabgleich. `Nichts senden` und `An Phoropter ausgeben` lassen den Rueckweg weiterhin offen.
- Doppelexporte werden nicht mehr ueber interne Verarbeitungshistorie blockiert, sondern organisatorisch ueber den konfigurierten Nachlauf vermieden. Wenn Eingangsdateien nach erfolgreichem Export im Importordner liegen bleiben, duerfen sie beim naechsten Scan erneut verarbeitet werden; das ist dann eine Profilkonfigurationsfolge.
- Auto-Zurueckandocken nach erfolgreicher Verarbeitung wiederhergestellt: Ein erfolgreicher Export setzt jetzt direkt einen terminalen AutoRedock-Abschluss fuer automatisch geoeffnete, nicht gepinnte Geraetefenster. Wiederholte Scan-Aktivitaet durch liegengebliebene Eingangsdateien schiebt den Rueckdock-Countdown nicht mehr endlos weiter; Pin verhindert AutoRedock weiterhin.
- Startfehler bei persistent abgedockten Geraeteanbindungsfenstern behoben: Floating-Fenster werden erst nach der sicheren MainWindow-Anzeige wiederhergestellt, `Owner` wird defensiv gesetzt, und ein Restore-Fehler dockt die Karte statt App-Abbruch sicher an.
- Floating-Geraeteanbindungsfenster stabilisiert: Schliessen per `X` dockt sicher zurueck, ohne rekursives `Close()`, Radar-/Scanbalken ist im Floating-Fenster sichtbar, und die Scanintervall-Buttons `-`/`+` sind dort ebenfalls verfuegbar.
- Textbuttons fuer Floating-Geraeteanbindungsfenster durch kompakte Symbolbuttons ersetzt: `🗗` fuer Abdocken/Andocken, `📌` fuer Position merken und `🔝` fuer Immer im Vordergrund; Funktion und State bleiben unveraendert.
- Symbol- und Scanintervall-Buttons in Geraeteanbindungsfenstern optisch beruhigt: transparente Button-Templates ersetzen die graue Standard-WPF-Fläche; Hover, Pressed und aktive Toggle-Zustaende bleiben sichtbar.
- Pin/TopMost fuer Floating-Geraeteanbindungsfenster getrennt: Floating-Fenster verwenden keinen gemeinsamen WPF-Owner mehr, und `ApplyState` nimmt nur den State der eigenen Schnittstellenprofil-ID an. AR360 und ARK1S koennen unabhaengig gepinnt werden.
- Soundausloesung fuer Geraeteanbindungsfenster korrigiert: Der Signalton haengt nicht mehr an deduplizierten Monitoring-Textmeldungen, wird nur noch bei stabil erkannter Geraetedatei gespielt und funktioniert bei spaeteren Durchlaeufen erneut.
- NT530P-MEDISTAR-Ausgabe korrigiert: `6205` beginnt mit `Tonometrie`, `6220` mit `Pachymetrie`, die Tonometrie wird in mehrere Feldzeilen aufgeteilt, und der alte Zusatz `/ EV:{000000003B} NT-530P Messung` entfaellt.
- Nachlauf nach erfolgreichem Export mit XDT-Mehrfachanhaengen korrigiert: bekannte AIS-/Geraetedateien werden wieder gemaess Profilregel entfernt/archiviert, Monitoring-Karten bleiben nicht auf alten Eingangsdaten oder falschem Anhang-Timeout stehen, und neue Dateien mit gleichem Namen werden nach Reset wieder erkannt.
- AutoDetach nach wiederholten Durchlaeufen korrigiert: AIS-/Geraete-/Paar-Scanereignisse tragen nun die Dateiversion im internen Event-Key, sodass neue Dateien mit gleichem Namen erneut das profilbezogene Floating-Fenster nach vorne holen, waehrend identische Scan-Wiederholungen weiter dedupliziert bleiben.
- AutoDetach nach Reset + manuellem Andocken korrigiert: der Monitoring-Dedupe-State wird beim Vorgangsreset profilbezogen freigegeben, sodass ein neuer AIS-Dateieingang nach `↺` und `X` das betroffene Geraetefenster wieder automatisch oeffnet.
- AttachmentOnly-/Dokumentgeraete-Konfiguration im Tab `Schnittstellenprofile` vereinfacht: Bei Dokumentgeraeten ist der Geraete-/Dokument-Importordner der massgebliche Dateieingang, separate XDT-Anhang-Importfelder werden ausgeblendet, die Abschlussoptionen bleiben sichtbar und die XDT-Anhang-Layoutueberlagerung wurde behoben.
- Mehrzeiliger Dokumentationstext fuer Dokumentgeraete erzeugt nun je Textzeile eine eigene `6227`-Feldzeile; leere Zeilen werden ausgelassen und es entstehen keine nackten Textzeilen mehr in der XDT-Datei.
- Manuelle AttachmentOnly-Bestaetigung korrigiert: Der Dialog `Dokumente uebertragen` bleibt bis zum Klick auf `Uebertragen` offen, weitere stabile Dateien werden in der Liste ergaenzt und erst die bewusste Bestaetigung startet den Export.
- `Pruefung vor Aktivierung` nutzt jetzt die aktuellen UI-Entwurfswerte auch vor dem Speichern und zeigt die Ordner-Erreichbarkeit als `Ja`, `Nein`, `Pfad fehlt` oder `Nicht geprueft`; nicht erreichbare, aber eingetragene Ordner bleiben Warnung/Hinweis statt harter Blocker.
- Dokumentanhang-Livefehler behoben: AttachmentOnly erzwingt zur Laufzeit die Anhangverarbeitung ueber den Dokument-Importordner, auch wenn alte gespeicherte technische Anhang-Flags deaktiviert sind. Ein Dokumentgeraet schreibt dadurch wieder `6302`/`6303`/`6305` je Datei und erzeugt keine AIS-only-XDT ohne Dokumentlinks.
- Die Dokumentanhang-UI zeigt nun eine profilbezogene Option `Dokumentationstext erfassen`; bei deaktivierter Option werden Anhaenge trotzdem exportiert, aber kein `6227` und kein Texteingabefeld erzeugt. Die internen `6302`-`6305`-Templatefelder werden fuer AttachmentOnly ausgeblendet und mit sicheren Defaults weiterverwendet.
- Dokumentanhang-Beschreibungen pro Datei ergaenzt: Der Dialog `Dokumente uebertragen` zeigt je Datei eine eigene Zeile mit Dateisymbol, Dateiname und Beschreibung. Dieser Text wird als `6304` zur jeweiligen Datei ausgegeben; ohne Eingabe wird der Originaldateiname verwendet, `6305` bleibt der technische Zielpfad.
- Die manuelle Dokumentuebergabe aktualisiert offene Dialoge nur noch inkrementell: neue stabile Dateien werden ergaenzt, vorhandene Beschreibungstexte bleiben erhalten und der Polling-Scan baut die Liste nicht mehr komplett neu auf.
- Texteingabe im Dialog `Dokumente uebertragen` entkoppelt: TextBoxen schreiben nicht mehr bei jedem Tastendruck ins ViewModel, offene Dialoge ueberspringen die schwere automatische Paarverarbeitung bei Folgescans und erhalten neue Dateien nur noch aus dem bereits stabilen Scan-Ergebnis.
- Start-/UI-Logik fuer Dokumentanhaenge korrigiert: `MEDISTAR + Manuelle Dokumentuebergabe` dockt beim Start der Ueberwachung nicht mehr ohne AIS-Datei ab, `MEDISTAR + Dokumentanhang` zeigt vor Vorgangsbeginn keinen roten `XDT-Anhang fehlt`-Status, und der Dialog `Dokumente uebertragen` nutzt den verfuegbaren Listenplatz besser.
- Dialog `Dokumente uebertragen` komfortabler gemacht: Das Fenster ist standardmaessig TopMost und kann per `TOP` umgeschaltet werden; Dateien koennen neben Drag-&-Drop und Dateiauswahl auch per `Strg+V`, Kontextmenue oder `Aus Zwischenablage` aus einer Datei-Zwischenablage ergaenzt werden.
- Manuelle Dokumentuebergabe oeffnet bei AIS-Eingang nur noch den Dialog `Dokumente uebertragen`; das gruene Geraeteanbindungsfenster bleibt fuer diesen Sondermodus angedockt, waehrend alle anderen Geraeteanbindungen ihr AutoDetach-Verhalten behalten. Der Dialog wurde optisch an die gruenen Geraeteanbindungsfenster angeglichen.
- Manuelle Dokumentuebergabe fuer wiederholte AIS-Laeufe korrigiert: Nach `Uebertragen` wird der interne Dialog-/Confirmation- und Coordinator-State des Profils freigegeben, sodass ein neuer AIS-Vorgang auch bei gleichem Dateinamen wieder einen leeren Dialog `Dokumente uebertragen` startet.
- LM7-Live-Export korrigiert: alte persistierte BuiltIn-Exportprofile mit `Device.R/LM/Median/...`-Pfaden werden beim Katalogstart gezielt auf die Parser-Pfade `Device.Measure[@Type='LM']/LM/R/MedistarLine` und `Device.Measure[@Type='LM']/LM/L/MedistarLine` repariert; leere Prisma-/PD-/ADD-Fragmente erscheinen dadurch nicht mehr im LM7-Export.
- KR800S-Live-Export korrigiert: alte persistierte BuiltIn-Exportprofile mit root-prefixed Einzelplatzhaltern und Keratometer-Regeln ueber `6228` werden beim Katalogstart gezielt auf die vorbereiteten `MedistarLine`-Pfade repariert. REF bleibt `6228`, KM wird korrekt ueber `6221` ausgegeben, SBJ bleibt konservativ `6227`, und leere Platzhalterzeilen wie `R.:S= Z=*` oder `KR: K1=* K2=*` entstehen nicht mehr.
- KR800S-SBJ-Ausgabe lesbarer gemacht: `Subjektive Refraktion Full Correction FAR/NEAR:` wird nun als eigene `6227`-Headerzeile ausgegeben, die Messwertzeile folgt als separate `6227`-Zeile. REF-`6228` und KM-`6221` bleiben unveraendert.
- Praktische MEDISTAR-Validierung fuer TOPCON KR800S dokumentiert: Serial0036 wurde mit anonymisierter XDT-Rueckgabe inklusive `6228` REF, `6221` KM und getrennten `6227`-SBJ-Zeilen protokolliert; Serial0426 ist mit den testseitig validierten REF/KM/SBJ-Erwartungswerten dokumentiert.
- TOPCON TRK2P-BuiltIns gegen Altzustaende abgesichert: persistierte BuiltIn-Geraete-/Exportprofile mit alten root-prefixed Pfaden oder unpassenden TM-/CCT-Regeln werden gezielt repariert; UserDefined-Profile bleiben unveraendert.
- TOPCON TRK2P-Tonometrie/Pachymetrie an den NT530P-Stil angepasst: `6205` beginnt mit `Tonometrie` und wird in einzelne lesbare Mess-/Parameter-/Listenzeilen aufgeteilt; `6220` beginnt bei vorhandenen CCT-Werten mit `Pachymetrie`. REF-`6228`, KM-`6221` und optionaler SBJ-`6227`-Pfad bleiben unveraendert.
- TOPCON TRK2P-Teilmessungen korrigiert: TM/CCT-only-Dateien ohne REF/KM/SBJ werden nicht mehr beim Mapping abgebrochen, sondern erzeugen nur die vorhandenen `6205`-/`6220`-Zeilen. Fehlende vorbereitete Messbloecke gelten generisch als optional, solange mindestens eine verwertbare Geraetezeile exportiert wird; CCT-ERROR-Eintraege werden ignoriert und fehlende CCT-Averages aus gueltigen Werten gemittelt.
- Praktische MEDISTAR-Validierung fuer TOPCON TRK2P dokumentiert: vollstaendige REF/KM/TM/CCT-Datei und TM/CCT-only-Teilmessdatei wurden mit anonymisierter XDT-Rueckgabe protokolliert; Teilmessungen ohne REF/KM/SBJ erzeugen nur `6220`/`6205`, keine leeren Platzhalter und keinen Exportabbruch.
- Generischen Nachlauf korrigiert: Erfolgreich verarbeitete AIS-/Geraetedateien werden nur gemaess Profilregel archiviert, verschoben oder entfernt. Eine fruehere Verarbeitung ist keine Sperre mehr; Wiederholungen werden durch saubere Importordner beziehungsweise Archiv-/Entfernen-Konfiguration organisatorisch vermieden.
- Templatepaket-Importvorschau in der WPF-App stabilisiert: Paketlesen und Vorschau-Erstellung laufen nun ueber einen UI-nah testbaren Preview-Service, der Import-Button ist waehrend der Vorschau gesperrt, und unveraenderte ComboBox-Auswahlereignisse bauen die Vorschau nicht erneut rekursiv auf.
- Templatepaket-Importvorschau benutzerfreundlicher gemacht: Konflikte und BuiltIn-Schutz starten nun mit `Ueberspringen` als sicherem Standard, `Als Kopie importieren` muss bewusst gewaehlt werden, Zielnamen fuer Kopien sind editierbar und leere/doppelte Zielnamen blockieren die Uebernahme verstaendlich.
- Abhaengigkeitsauflösung und Importzusammenfassung im Templatepaket-Import klarer formuliert, inklusive Hinweis auf Zielbereiche fuer importierte Profile und weiterhin fehlende automatische Aktivierung.
- Templatepaket-Importvorschau weiter verstaendlicht: leere Abhaengigkeitsauflösung zeigt nun einen deutschen Leerzustand, sichtbare Hinweise/Warnungen sind deutsch lokalisiert und doppelte Hinweise werden reduziert.
- Button-Anordnung im Bereich `Exportregeln` verstaendlicher gruppiert: Regel-Aktionen stehen bei der Regelliste, Exportprofil-Speichern/-Loeschen beim Profilentwurf; deaktivierte Loeschbuttons erklaeren BuiltIn- oder Referenzschutz per Tooltip.
- `Neues Exportprofil anlegen` im Tab `Profile & Templates` sichtbar repariert: Der Button startet nun einen leeren Exportprofil-Entwurf, setzt einen eindeutigen Profilnamen, zeigt eine Status-/Logmeldung und speichert weiterhin erst nach bewusstem Klick auf `Entwurf als neues Exportprofil speichern`.

### Nicht umgesetzt

- Noch nicht enthalten sind Autostart, Windows-Dienst, UI-Einstellung fuer die Rueckdock-Zeit, ein sichtbarer Countdown-Hinweis fuer abdockbare Geraeteanbindungsfenster sowie Vorschaukachel-Galerien und Drag-&-Drop fuer automatisch liefernde Dokumentgeraete.

### Dokumentation

- Geraete-/Template-Matrix ergaenzt: BuiltIn-Geraeteprofile, MEDISTAR-Exportprofile, vorhandene Testdaten, Templatepaket-Luecken und V1-Prioritaeten fuer konkrete Geraete-/Templatearbeit sind kompakt dokumentiert.
- Templatepaket-Kandidat `docs/TEMPLATEPAKET_MEDISTAR_NIDEK_LM7.md` dokumentiert; Lensmeter-XDT-Rueckgabe ist praktisch validiert, Prisma-/PD-Faelle und offizielles ZIP-Artefakt bleiben offen.
- Templatepaket-Kandidat `docs/TEMPLATEPAKET_MEDISTAR_NIDEK_AR360.md` dokumentiert; ARMedian ist fuer AR360 massgeblich, die abweichende Achse aus der bereitgestellten MEDISTAR-TXT wird nicht kuenstlich uebernommen.
- E2E-Protokoll `docs/E2E_TESTPROTOKOLL_MEDISTAR_AR360.md` ergaenzt; die dokumentierte Ausgabe ist anonymisiert und enthaelt keine Kunden-/Patientendaten oder Live-Pfade.
- E2E-Protokoll `docs/E2E_TESTPROTOKOLL_MEDISTAR_LM7.md` ergaenzt; die LM7-Lensmeter-Rueckgabe ist anonymisiert dokumentiert und enthaelt keine Kunden-/Patientendaten oder Live-Pfade.
- ARK1S und AR360 als stabile Referenzpakete in Matrix und Paketdokumenten markiert; offizielle ZIP-Artefakte werden erst nach `docs/TEMPLATEPAKET_RELEASE_REGEL.md` dauerhaft abgelegt.
- Kleine Release-Regel fuer offizielle Templatepaket-ZIPs dokumentiert, inklusive selektivem Exporttest, Import-/DryRun-Test, Praxis-App-Importpruefung, BuiltIn-Schutz und Daten-/Pfadpruefung.
- Profilumbenennung dokumentiert: Templatepaket-ZIP-Dateien werden weiterhin ueber Dateinamen/Release-Regel versioniert; fuer Importvorschau-Zielnamen bleibt die bestehende Vorschau-Bearbeitung massgeblich, es wurde keine separate Templatepaket-Verwaltung eingefuehrt.
- Templatepaket-Kandidat `docs/TEMPLATEPAKET_MEDISTAR_DOKUMENTANHANG.md` dokumentiert; er beschreibt automatisch liefernde Dokumentgeraete ohne Messwerte, optionalen `6227`-Text, mehrere `6302`-`6305`-Anhaenge und die bewusst offenen Komfortthemen Vorschau und Datei-Kommentare.
- Templatepaket-Kandidat `docs/TEMPLATEPAKET_MEDISTAR_MANUELLE_DOKUMENTUEBERGABE.md` dokumentiert; er beschreibt die AIS-gestartete manuelle Dateiauswahl mit Drag-&-Drop, Mehrfachauswahl, pro-Datei-`6304` und sicherer Copy-Uebergabe ohne Messwertparser.
- Praxisabnahme `docs/PRAXISABNAHME_GERAETEFENSTER_V1.md` ergänzt: Systray, manuelles und automatisches Abdocken, Auto-Zurueckandocken, Pin/Position, Signalton, Reset und Sicherheitsgrenzen der Geraeteanbindungsfenster sind als V1-Funktionsblock abgenommen.
- Templatepaket-Kandidat `docs/TEMPLATEPAKET_MEDISTAR_TOPCON_CV5000.md` und Fixture-basiertes Protokoll `docs/E2E_TESTPROTOKOLL_MEDISTAR_TOPCON_CV5000.md` ergaenzt; sie trennen die Richtung AIS/MEDISTAR -> Phoropter-Import-XML von der Richtung Phoropter -> MEDISTAR-Rueckgabe und markieren die Livevalidierung als offen.
- Projektleitlinie geschaerft: fertige Geraeteprofile und Templatepakete haben Vorrang vor Baukasten-Nutzung; der Aktivierungsassistent ruht bis auf Weiteres als read-only vorbereiteter Stand.
- Offizielle Paketvorlage `docs/TEMPLATEPAKET_MEDISTAR_NIDEK_ARK1S.md` fuer das erste Referenzpaket MEDISTAR + NIDEK ARK1S ergaenzt; sie nutzt das bestehende ZIP-Templatepaket-Format, enthaelt keine Kunden-/Patientendaten und keine Live-Pfade.
- ARK1S-Paketvorlage dokumentiert jetzt den automatisierten Export-/Import-Testweg; die ZIP wird weiterhin nicht manuell eingecheckt und bleibt bis zur Release-Regel ein temporaeres Testartefakt.
- Templatepaket-Dokumentation ergaenzt: MEDISTAR + NIDEK ARK1S wird ueber den selektiven Export aus dem vorhandenen Schnittstellenprofil erzeugt; Vollkatalog-Exporte sind nicht mehr der Standardweg.
- `docs/ROADMAP.md` ergänzt als aktualisierte Roadmap für den Stand nach XDT-Anhang-Ausbau, Baukasten-Testexport, Paket-Wartelogik und UI-Refactoring.
- Aktuellen Entwicklungsstand zu XDT-Anhängen für AIS, Test & Vorschau, Testexport, Dateistabilität, konfigurierbarem Scan-Intervall und zweistufiger Paketlogik dokumentiert.
- Templatepaket-Import um aktuellen Stand ergänzt: Konfliktanalyse, Importplan, Dry-Run, UI-Vorschau, sichere Benutzerentscheidungen und explizite UserDefined-Übernahme.
- BuiltIn-Schutz, deaktiviertes `ReplaceExisting`, inaktive importierte Schnittstellenprofile und deaktiviertes `IsAttachmentProcessingEnabled` bei importierten Schnittstellenprofilen dokumentiert.
- E2E-nahe Tests für den sicheren Templatepaket-Importfluss dokumentiert.
- Praxisvalidierung MEDISTAR + NIDEK ARK1S + XDT-Anhang-Link dokumentiert.
- Pflicht-XDT-Anhang-Testfälle erfolgreich geprüft: Anhang vorhanden und Anhang fehlt/Fehlerfall.
- Projektstandsicherung zum aktuellen Aktivierungsassistenten ergänzt: Service-Kette, Statuslogik, Sicherheitsgrenzen, UI-Stand, letzter Build-/Teststand und offene Schritte dokumentiert.
- Entscheidungsnotiz `docs/AKTIVIERUNG_ENTSCHEIDUNGSNOTIZ.md` ergänzt; sie strukturiert die offenen fachlichen Entscheidungen vor einer späteren produktiven Schnittstellenprofil-Aktivierung.
- Entscheidungsnotiz radikal auf schlanke V1 gestrafft: spaetere Aktivierung nur fuer frisch geladene UserDefined-Profile mit Status `Ready`, ohne Blocker und ohne Warnungen; `ReadyWithWarnings`, Warnungsbestaetigung, Audit, Deaktivierung, Fingerprint und Paketsonderlogik sind nicht V1.
- Statisches UI-Pruefprotokoll `docs/UI_PRUEFPROTOKOLL_AKTIVIERUNGSASSISTENT.md` ergänzt; die laufende WPF-UI wurde in der Codex-Umgebung nicht praktisch bedient, die Preview-/Dialogpfade wurden statisch auf reine Vorschau und fehlende Aktivierungswirkung geprüft.
- Praktische Windows-Sichtpruefung des Dialogs `Aktivierung vorbereiten` im UI-Pruefprotokoll dokumentiert; scrollbares Preview-Fenster, gestraffte Texte und weiterhin fehlende produktive Wirkung sind fuer den Vorschau-Status abgenommen.
- Klarstellung: Die Änderungen beschreiben den aktuellen Dokumentations- und Vorbereitungsstand; Produktivlogik, BuiltIn-Profile und Exportprofile wurden dadurch nicht geändert.

## 0.1.0-prototype

Erster stabiler Prototyp der XdtDeviceBridge.

### Enthalten

- WPF-Desktop-App
- MEDISTAR-GDT/XDT-Eingang
- NIDEK ARK1S XML-Eingang
- MEDISTAR-kompatibler XDT-Export
- `8000=6310` Steuerfeld
- `8402` Untersuchungsart aus AIS
- `6228` Ergebniszeilen
- Formatfunktionen fuer ophthalmologische Messwerte
- manuelle Verarbeitung
- Schnittstellenprofile
- Profil-/Template-Grundmodell
- Template-Export und Template-Importpruefung
- Exportregel-Baukasten im Entwurfsmodus
- manuell startbare Ueberwachung
- optionale automatische Verarbeitung
- Archivierung per Kopie oder Verschieben
- Nachlauf ueber Archivieren, Verschieben oder Entfernen statt interner Duplikatsperre
- Fehlerablage mit `error.txt`
- Offline-Lizenz-Prototyp
- lizenzierte Geraete-/Anbindungsanzeige
- Karenzzeitmodell fuer neue Anbindungen
- Testsuite mit aktuellem Stand

### Nicht enthalten / bewusst noch offen

- Windows-Dienst
- Autostart
- echter FileSystemWatcher
- produktive Lizenzsperre
- digitale Signaturpruefung fuer Lizenzdateien
- Online-Lizenzierung
- vollstaendiger Profil-Assistent fuer unbekannte Geraete
- Geräte-Dateianhang-Import und MEDISTAR externer Link über XDT
- Dokument-/Dateianhang-Template
- selbst erzeugte PDF-Protokolle
- produktive Archivloeschung im Hintergrund
- SQLite-Speicherung
- produktive Templatepaket-Uebernahme mit Konfliktloesung
- Installer / Deployment

### Dokumentationsstand nach 0.1.0-prototype

- Geräte-Dateianhänge und externe AIS-Link-Übergabe sind als verbindliche Zielanforderung dokumentiert.
- Für MEDISTAR ist die Link-Übergabe über XDT-Felder `6302`, `6303`, `6304` und `6305` beschrieben.
- Die Beispieldatei `XDT Übergabe externer Link.txt` wurde fachlich ausgewertet; die MEDISTAR-interne `EV:{...}`-Anzeige wird klar von der XDT-Feldübergabe getrennt.
- Keine Implementierung dieser Funktion in `0.1.0-prototype`.
